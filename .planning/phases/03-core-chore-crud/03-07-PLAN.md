---
phase: 03-core-chore-crud
plan: 07
type: execute
wave: 3
depends_on: ["03-04", "03-05"]
files_modified:
  - frontend-v2/src/pages/AssignmentsPage.tsx
  - frontend-v2/src/pages/DashboardPage.tsx
  - frontend-v2/src/App.tsx
  - frontend-v2/src/__tests__/AssignmentsPage.test.tsx
autonomous: true
requirements: [CHORE-02, CHORE-03, CHORE-04, CHORE-06]
must_haves:
  truths:
    - "Parent sees all family assignments at /assignments with user filter (CHORE-06)"
    - "Parent can create an assignment by selecting a template, user, and due date (CHORE-02)"
    - "Parent can edit an assignment's due date or reassignee (CHORE-03)"
    - "Parent can delete a pending assignment with confirmation (CHORE-04)"
    - "Dashboard shows Upcoming Chores section for the logged-in user"
    - "App.tsx has routes for /templates, /assignments, /my-chores with correct role gates"
    - "AssignmentsPage uses FilterBar with user filter to scope to one child or all"
  artifacts:
    - path: "frontend-v2/src/pages/AssignmentsPage.tsx"
      provides: "Parent-only assignment management with filter and CRUD"
    - path: "frontend-v2/src/pages/DashboardPage.tsx"
      provides: "Updated dashboard with NavBar and Upcoming Chores section"
    - path: "frontend-v2/src/App.tsx"
      provides: "Route definitions for all Phase 3 pages"
    - path: "frontend-v2/src/__tests__/AssignmentsPage.test.tsx"
      provides: "Unit tests for assignment page states"
  key_links:
    - from: "frontend-v2/src/App.tsx"
      to: "Page components + ProtectedRoute"
      via: "React Router <Route> with <ProtectedRoute> wrapper"
      pattern: "path=\"/templates\".*TemplatesPage"
---

<objective>
Build the AssignmentsPage (parent-only assignment management), update DashboardPage with the NavBar component and Upcoming Chores section, and wire all Phase 3 routes in App.tsx.

Purpose: AssignmentsPage delivers CHORE-02 (create), CHORE-03 (edit), CHORE-04 (delete), and CHORE-06 (parent views all). Dashboard connects the user to their immediate tasks. App.tsx makes all pages accessible via navigation.

Output: Three modified files, one test file. The full Phase 3 frontend is now navigable and feature-complete.
</objective>

<execution_context>
@/home/thitar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/thitar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-chore-crud/03-UI-SPEC.md
@.planning/phases/03-core-chore-crud/03-VALIDATION.md
@frontend-v2/src/App.tsx
@frontend-v2/src/pages/DashboardPage.tsx
@frontend-v2/src/pages/LoginPage.tsx
@frontend-v2/src/pages/TemplatesPage.tsx
@frontend-v2/src/pages/MyChoresPage.tsx
@frontend-v2/src/hooks/useAuth.tsx
@frontend-v2/src/hooks/useAssignments.tsx
@frontend-v2/src/hooks/useTemplates.tsx
@frontend-v2/src/components/NavBar.tsx
@frontend-v2/src/components/FilterBar.tsx
@frontend-v2/src/components/StatusBadge.tsx
@frontend-v2/src/components/ConfirmDelete.tsx
@frontend-v2/src/components/ProtectedRoute.tsx

<interfaces>
From useAssignments (created in 03-04):
```typescript
export function useAssignments(): {
  assignments: Assignment[]; isLoading: boolean; error: Error | null;
  createAssignment: (data: { templateId: number; userId: number; dueDate: string }) => Promise<Assignment>;
  updateAssignment: (id: number, data: { userId?: number; dueDate?: string }) => Promise<Assignment>;
  deleteAssignment: (id: number) => Promise<{ deleted: true }>;
  isCreating: boolean; isUpdating: boolean; isDeleting: boolean;
}
```

From useTemplates (created in 03-04):
```typescript
export function useTemplates(): { templates: Template[]; ... }
```

From useAuth:
```typescript
export function useAuth(): { user: User | null; ... }
// User has: id, email, name, role, color
```

From ProtectedRoute:
```typescript
export function ProtectedRoute({ children, requiredRole }: { children: React.ReactNode; requiredRole?: 'PARENT' | 'CHILD' })
```

From NavBar:
```typescript
export function NavBar() // Uses useAuth internally for role-conditional links
```

UI-SPEC contracts:
- Page 2: Assignments (Parent Only) — §Page Contracts > Page 2
- Page 4: All Assignments View — merged into /assignments with user filter
- Page 5: Dashboard — §Page Contracts > Page 5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AssignmentsPage with filter, create/edit/delete</name>
  <files>frontend-v2/src/pages/AssignmentsPage.tsx, frontend-v2/src/__tests__/AssignmentsPage.test.tsx</files>

  <read_first>frontend-v2/src/pages/TemplatesPage.tsx, frontend-v2/src/pages/MyChoresPage.tsx, frontend-v2/src/hooks/useAssignments.tsx, frontend-v2/src/hooks/useTemplates.tsx, frontend-v2/src/hooks/useAuth.tsx</read_first>

  <behavior>
    - Test: renders loading spinner
    - Test: renders empty state ("No assignments yet" + "Assign Chore" button)
    - Test: renders assignment list with template name, assigned user, due date, status, points
    - Test: user filter limits visible assignments to selected user
    - Test: clicking "Assign Chore" shows create form with template select, user select, date input
    - Test: clicking edit icon shows form pre-filled with current assignment data
    - Test: clicking delete icon shows inline confirmation panel
    - Test: error state with retry
  </behavior>

  <action>
    **First, create `frontend-v2/src/__tests__/AssignmentsPage.test.tsx`:**
    Follow the scaffold.test.tsx pattern for mocking. Use `vi.mock` to mock `useAssignments`, `useTemplates`, and `useAuth` hooks. Tests must cover:
    - Loading state: renders spinner with "Loading assignments..."
    - Empty state: "No assignments yet" heading with "Assign Chore" button
    - Populated state: renders assignment rows with template name, assigned user, due date, status badge, points
    - User filter: changing user filter limits visible assignments
    - Create form: "Assign Chore" opens form with template select, user select, date input
    - Edit form: clicking edit icon shows form pre-filled with assignment data
    - Delete confirmation: clicking delete icon shows inline ConfirmDelete panel
    - Error state: renders retry button

    Run `cd frontend-v2 && npx vitest run -- src/__tests__/AssignmentsPage.test.tsx` — tests must FAIL (RED gate, implementation not yet written).

    **Then, build AssignmentsPage per UI-SPEC Page 2 + Page 4 (merged).** Page 4 merges into /assignments — the user filter's "All Users" option already provides the all-family view (CHORE-06).

    **Imports:** `{ useState, useMemo, useEffect }` from react, `{ useAssignments, useTemplates, useUsers, useAuth }` from hooks, `{ NavBar, FilterBar, StatusBadge, ConfirmDelete }` from components, `{ Plus, Pencil, Trash2 }` from lucide-react.

    **Data:** `useAssignments()` for assignment CRUD, `useTemplates()` for template dropdown in create form, `useUsers()` for user dropdown in create form (CHORE-02 — fixes empty-state: with zero assignments, users are still available from GET /api/users), `useAuth()` for current user.

    **Filter state (D-09):**
    - `statusFilter` (default `'all'`), `userFilter` (default `'all'`), `dateFrom` (default: current month first day), `dateTo` (default: current month last day).

    **Create/Edit form state:**
    - `showForm`, `editingAssignment` (null = create mode), `formError`, `successMessage` (auto-dismissed via 3s `setTimeout` with `useEffect` cleanup to prevent unmount warning).
    - Form fields: `selectedTemplateId`, `selectedUserId`, `dueDate`.

    **Delete state:** `deletingAssignmentId` (number | null).

    **Filtered assignments (D-09):** `useMemo` with status, date range, AND user filter: `if (userFilter !== 'all' && a.assignedToId !== parseInt(userFilter)) return false`.

    **Users list for dropdowns:** `useUsers()` returns `users: UserSummary[]` (from Plan 03-04). The FilterBar's user filter uses this list. The create form user select filters to `users.filter(u => u.role === 'CHILD')` for the assignment create dropdown. This fixes the CHORE-02 empty-state: even with zero existing assignments, `GET /api/users` returns all family members, so the dropdown is never empty.

    **Layout:**
    - Outer: `<div className="min-h-screen bg-gray-50"><NavBar />`.
    - Main: `<main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">`.
    - Title: `<h2 className="text-2xl font-bold text-gray-900 mb-4">Chore Assignments</h2>`.
    - Success message (conditional).
    - "Assign Chore" button (primary) per UI-SPEC copywriting.

    **FilterBar** with `showUserFilter={true}`:
    - `users` prop: `users` from `useUsers()`. The FilterBar renders a dropdown labeled "All Users" (value="all") + each user by name.

    **Create/Edit form** (UI-SPEC Page 2 form fields):
    - Template select: `<select value={selectedTemplateId} onChange={...} className="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-primary-ring bg-white">` with options mapped from `templates`.
    - User select: `<select value={selectedUserId} onChange={...}>` with options from unique assignedTo users.
    - Due date: `<input type="date" ...>`.
    - Save/Cancel buttons per TemplatesPage pattern using `isCreating`/`isUpdating` from hooks.

    **Table** (UI-SPEC §6 assignment variant — 5 cols: chore name+assigned to, due date, status, points, actions):
    - Chore name column: `{assignment.template.title}` (bold) + `{assignment.assignedTo.name}` below in `text-sm text-gray-500` with a small colored dot (`style={{ backgroundColor: assignment.assignedTo.color }}`).
    - Due date: formatted `MMM D, YYYY`. Overdue check: `text-red-600 font-bold` for past due + PENDING.
    - Status: `<StatusBadge status={assignment.status} overdue={isOverdue} />`.
    - Points: For completed: `{assignment.pointsAwarded} pts` (D-02 snapshot). For pending: `{assignment.template.points} pts` (D-01: reads from template).
    - Actions: edit icon + delete icon for all assignments (even completed ones — parent might need to uncomplete then delete per D-13). Icon buttons with `aria-label`.

    **Delete flow:** ConfirmDelete with `message="This assignment will be permanently removed. The chore template will not be affected. Continue?"`, `deleteLabel="Delete Assignment"`, `keepLabel="Keep Assignment"`. On delete success, row removed, success message.

    **Note on completed assignments:** Completed assignments can't be deleted (backend returns 409 per D-13). The delete icon for completed assignments should show but the API call will fail with an error the user sees. Or better: disabled state on the icon for completed. Let me just let the backend error surface naturally — the user sees the error message from the API call.

    **Uncomplete not implemented in UI:** D-08 uncomplete is parent-only and backend-supported, but there's no UI button in this phase. A parent can uncomplete via API but the UI keeps it simple. This aligns with UI-SPEC which doesn't show an uncomplete action.
  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit src/pages/AssignmentsPage.tsx && npx vitest run -- src/__tests__/AssignmentsPage.test.tsx -- no TypeScript errors, tests pass</automated>
  </verify>

  <acceptance_criteria>
    - AssignmentsPage.test.tsx exists with 8 test cases covering all states (loading, empty, populated, filter, create, edit, delete, error)
    - All 8 AssignmentsPage tests pass
    - AssignmentsPage renders NavBar + page title "Chore Assignments"
    - FilterBar with user filter (All Users + individual family members), status filter, date range
    - User filter uses `useUsers()` for dropdown options (not extracted from assignment data)
    - "Assign Chore" button opens create form with template select, user select, date picker
    - Edit form pre-fills current assignment values
    - Delete shows inline confirmation with "Delete Assignment" / "Keep Assignment"
    - Status badges: yellow Pending, green Completed, red Overdue
    - Points: completed uses pointsAwarded (D-02), pending uses template.points (D-01)
    - Loading, empty, error states all covered (matching UI-SPEC copywriting)
    - TypeScript compilation: no errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update DashboardPage with NavBar and Upcoming Chores section</name>
  <files>frontend-v2/src/pages/DashboardPage.tsx</files>

  <read_first>frontend-v2/src/pages/DashboardPage.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/hooks/useAssignments.tsx, frontend-v2/src/hooks/useAuth.tsx</read_first>

  <action>
    Update DashboardPage per UI-SPEC Page 5 contract. The NavBar was already wired in Plan 03-05. Now add the "Upcoming Chores" section.

    **Current DashboardPage** (after Plan 03-05): Uses `<NavBar />`, has welcome heading with user name, Phase 3 TODO comments.

    **Changes:**
    - Import `{ useAssignments }` from `../hooks/useAssignments` and `{ useAuth }` from `../hooks/useAuth`.
    - Destructure: `const { assignments, isLoading, error } = useAssignments()`.
    - Below the welcome heading, add Upcoming Chores section.

    **Upcoming Chores section** (UI-SPEC Page 5):
    - Section heading: `<h2 className="text-base font-bold text-gray-900 mb-4">Upcoming Chores</h2>`.
    - Filter: take the logged-in user's PENDING assignments (from useAssignments), sort by `dueDate` ascending, take first 5.
    - Loading: inline spinner in the section.
    - Empty: `<p className="text-gray-500">No upcoming chores. Enjoy your free time!</p>`.
    - Populated: list of up to 5 assignment cards. Each card:
      - Chore name: `{assignment.template.title}` (bold).
      - Due date: formatted. Overdue → `text-red-600` with "Overdue" label. Due today → "Today" label. Future → relative format: "Tomorrow" for +1 day, "in {n} days" for 2-6 days, or formatted date for further out.
      - Status badge: `<StatusBadge status={assignment.status} overdue={isOverdue} />`.
      - Points: `{assignment.template.points} pts`.
    - Card style: `<div className="bg-white rounded-lg shadow-sm p-4 flex items-center justify-between">` per UI-SPEC card pattern.
    - Layout: `<div className="space-y-3">` for card stack.

    **Error state in Upcoming section:** If `error` is truthy, show: `<p className="text-red-600 text-sm">Unable to load upcoming chores.</p>`.

    **Filter logic:**
    ```typescript
    const upcoming = useMemo(() => {
      if (!user || !assignments.length) return []
      return assignments
        .filter(a => a.assignedToId === user.id && a.status === 'PENDING')
        .sort((a, b) => new Date(a.dueDate).getTime() - new Date(b.dueDate).getTime())
        .slice(0, 5)
    }, [assignments, user])
    ```

    **Remove Phase 3 TODOs:** Replace `{/* TODO: Phase 3 — add chore list summary */}` and `{/* TODO: Phase 5 — wire points display from useAuth().user.points */}` with the actual Upcoming Chores section.

    Keep the welcome heading: `<h2 className="text-2xl font-bold mb-4">Welcome, {user?.name}!</h2>`.
  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit src/pages/DashboardPage.tsx -- no TypeScript errors</automated>
  </verify>

  <acceptance_criteria>
    - DashboardPage uses NavBar component (no inline nav)
    - Welcome heading remains: "Welcome, {name}!"
    - Upcoming Chores section heading: "Upcoming Chores"
    - Shows up to 5 soonest PENDING assignments for the logged-in user
    - Overdue chores: due date in red-600 with "Overdue" label
    - Due today: "Today" label
    - Future: relative date or formatted date
    - Empty: "No upcoming chores. Enjoy your free time!"
    - Loading: inline spinner while assignments load
    - Error: "Unable to load upcoming chores." text
    - Phase 3/5 TODO comments removed
    - TypeScript compilation: no errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Phase 3 routes to App.tsx</name>
  <files>frontend-v2/src/App.tsx</files>

  <read_first>frontend-v2/src/App.tsx, frontend-v2/src/components/ProtectedRoute.tsx</read_first>

  <action>
    Wire all Phase 3 pages into the React Router configuration.

    **Current App.tsx:**
    ```tsx
    import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
    import { LoginPage } from './pages/LoginPage'
    import { DashboardPage } from './pages/DashboardPage'
    import { ProtectedRoute } from './components/ProtectedRoute'
    ```

    **Add imports:**
    ```tsx
    import { TemplatesPage } from './pages/TemplatesPage'
    import { AssignmentsPage } from './pages/AssignmentsPage'
    import { MyChoresPage } from './pages/MyChoresPage'
    ```

    **Add routes** inside `<Routes>`, AFTER the existing DashboardPage route (line 10-15) and BEFORE the catch-all `*` route (line 16):

    ```tsx
    <Route path="/templates" element={
      <ProtectedRoute requiredRole="PARENT">
        <TemplatesPage />
      </ProtectedRoute>
    } />
    <Route path="/assignments" element={
      <ProtectedRoute requiredRole="PARENT">
        <AssignmentsPage />
      </ProtectedRoute>
    } />
    <Route path="/my-chores" element={
      <ProtectedRoute>
        <MyChoresPage />
      </ProtectedRoute>
    } />
    ```

    - `/templates` and `/assignments` use `<ProtectedRoute requiredRole="PARENT">` (D-18: parent-only).
    - `/my-chores` uses `<ProtectedRoute>` without requiredRole (any authenticated user).
    - All three use `<BrowserRouter future={{ v7_startTransition: true, v7_relativeSplatPath: true }}>` which is already configured.

    **Route ordering:** Keep the existing logic: `/login` → `/` → new routes → `*`. The catch-all `*` redirects to `/`.

    Do NOT change any existing imports or the BrowserRouter configuration.
  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit src/App.tsx -- no TypeScript errors</automated>
  </verify>

  <acceptance_criteria>
    - App.tsx imports TemplatesPage, AssignmentsPage, MyChoresPage
    - `/templates` route wrapped in `<ProtectedRoute requiredRole="PARENT">`
    - `/assignments` route wrapped in `<ProtectedRoute requiredRole="PARENT">`
    - `/my-chores` route wrapped in `<ProtectedRoute>` (no requiredRole)
    - All three routes are between `/` and `*` catch-all
    - Existing routes (login, dashboard, catch-all) unchanged
    - TypeScript compilation: no errors
    - BrowserRouter future flags preserved
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (assignments) | POST/PUT/DELETE via hooks; backend enforces PARENT-only |
| client→API (complete) | POST via MyChoresPage hook; backend enforces ownership |
| Route→Component | ProtectedRoute gates parent-only pages at the router level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-14 | Elevation of Privilege | CHILD accesses /templates or /assignments via URL | mitigate | `<ProtectedRoute requiredRole="PARENT">` redirects to 403 page (defense at route level, not component level) |
| T-3-15 | Information Disclosure | Dashboard shows other users' chores | mitigate | `upcoming` filter only shows `assignedToId === user.id`; useAssignments hook returns role-scoped data from backend (D-10); defense in depth |
</threat_model>

<verification>
```bash
cd frontend-v2 && npx tsc --noEmit
```
All Phase 3 files must compile without TypeScript errors.

Full frontend test suite:
```bash
cd frontend-v2 && npx vitest run
```
All existing tests (scaffold + NavBar + new page tests) must pass.
</verification>

<success_criteria>
- AssignmentsPage: create, edit, delete with user filter (populated from useUsers()) and all states
- AssignmentsPage create form: user dropdown populated from GET /api/users, filtered to CHILD role (CHORE-02 empty-state fixed)
- DashboardPage: NavBar + Upcoming Chores section with overdue/today/future display
- App.tsx: 3 new routes with correct ProtectedRoute wrappers
- /templates and /assignments parent-only (CHILD sees 403)
- /my-chores accessible by any authenticated user
- Upcoming Chores shows only logged-in user's pending assignments
- All Phase 3 pages are reachable via navigation (NavBar links + direct URL)
- TypeScript compilation: no errors in any modified file
- Frontend test suite: all tests pass
</success_criteria>

<output>
Create `.planning/phases/03-core-chore-crud/03-07-SUMMARY.md` when done
</output>
