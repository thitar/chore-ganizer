---
phase: 03-core-chore-crud
plan: 06
type: execute
wave: 3
depends_on: ["03-04", "03-05"]
files_modified:
  - frontend-v2/src/pages/TemplatesPage.tsx
  - frontend-v2/src/pages/MyChoresPage.tsx
  - frontend-v2/src/__tests__/TemplatesPage.test.tsx
  - frontend-v2/src/__tests__/MyChoresPage.test.tsx
autonomous: true
requirements: [CHORE-01, CHORE-05, CHORE-07]
must_haves:
  truths:
    - "Parent sees Templates page at /templates with create, edit, delete actions (CHORE-01)"
    - "Authenticated user sees My Chores page at /my-chores with filter and Mark Complete button (CHORE-05, CHORE-07)"
    - "Completing a chore shows 'Completing...' disabled state then refreshes with green status"
    - "Both pages show loading spinner, error state with retry, and empty state with contextual message"
    - "All UI follows UI-SPEC contracts exactly (spacing, typography, color, copywriting)"
  artifacts:
    - path: "frontend-v2/src/pages/TemplatesPage.tsx"
      provides: "Parent-only template CRUD page"
    - path: "frontend-v2/src/pages/MyChoresPage.tsx"
      provides: "Role-scoped chore list with complete action"
    - path: "frontend-v2/src/__tests__/TemplatesPage.test.tsx"
      provides: "Unit tests for template page states"
    - path: "frontend-v2/src/__tests__/MyChoresPage.test.tsx"
      provides: "Unit tests for my chores page states"
  key_links:
    - from: "frontend-v2/src/pages/TemplatesPage.tsx"
      to: "frontend-v2/src/hooks/useTemplates.tsx"
      via: "useTemplates() hook"
      pattern: "import.*useTemplates"
    - from: "frontend-v2/src/pages/MyChoresPage.tsx"
      to: "frontend-v2/src/hooks/useAssignments.tsx"
      via: "useAssignments() hook"
      pattern: "import.*useAssignments"
---

<objective>
Build the TemplatesPage (parent-only template CRUD) and MyChoresPage (role-scoped chore list with complete flow). These are full page components using the shared components from Plan 03-05 and data hooks from Plan 03-04.

Purpose: TemplatesPage delivers CHORE-01 (template creation UI). MyChoresPage delivers CHORE-05 (child views own chores) and CHORE-07 (complete action with visual feedback).

Output: Two page components with full state coverage (loading, empty, error, populated), two test files.
</objective>

<execution_context>
@/home/thitar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/thitar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-chore-crud/03-UI-SPEC.md
@.planning/phases/03-core-chore-crud/03-VALIDATION.md
@frontend-v2/src/pages/LoginPage.tsx
@frontend-v2/src/pages/DashboardPage.tsx
@frontend-v2/src/hooks/useAuth.tsx
@frontend-v2/src/hooks/useTemplates.tsx
@frontend-v2/src/hooks/useAssignments.tsx
@frontend-v2/src/components/NavBar.tsx
@frontend-v2/src/components/FilterBar.tsx
@frontend-v2/src/components/StatusBadge.tsx
@frontend-v2/src/components/ConfirmDelete.tsx
@frontend-v2/src/api/templates.api.ts
@frontend-v2/src/api/assignments.api.ts
@frontend-v2/src/__tests__/scaffold.test.tsx

<interfaces>
From useTemplates.tsx (created in 03-04):
```typescript
export function useTemplates(): {
  templates: Template[]; isLoading: boolean; error: Error | null;
  createTemplate: (data: { title: string; points: number; category: string; description?: string }) => Promise<Template>;
  updateTemplate: (id: number, data: { title?: string; points?: number; category?: string; description?: string }) => Promise<Template>;
  deleteTemplate: (id: number) => Promise<{ deleted: true }>;
  isCreating: boolean; isUpdating: boolean; isDeleting: boolean;
}
```

From useAssignments.tsx (created in 03-04):
```typescript
export function useAssignments(): {
  assignments: Assignment[]; isLoading: boolean; error: Error | null;
  createAssignment: ...; updateAssignment: ...;
  completeAssignment: (id: number) => Promise<Assignment>;
  uncompleteAssignment: ...; deleteAssignment: ...;
  isCreating: boolean; isUpdating: boolean; isCompleting: boolean; isUncompleting: boolean; isDeleting: boolean;
}
```

From useAuth.tsx:
```typescript
export function useAuth(): { user: User | null; ... }
```

UI-SPEC page contracts:
- Page 1: Chore Templates (Parent Only) — §Page Contracts > Page 1
- Page 3: My Chores (Child + Parent) — §Page Contracts > Page 3
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TemplatesPage with full state coverage</name>
  <files>frontend-v2/src/pages/TemplatesPage.tsx, frontend-v2/src/__tests__/TemplatesPage.test.tsx</files>

  <read_first>frontend-v2/src/pages/LoginPage.tsx, frontend-v2/src/hooks/useTemplates.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/components/ConfirmDelete.tsx</read_first>

  <behavior>
    - Test: renders loading spinner when templates are loading
    - Test: renders empty state ("No chore templates yet" + "Create your first chore template...")
    - Test: renders template list with title, category, points per row
    - Test: clicking edit icon shows form with pre-filled values
    - Test: clicking delete icon shows inline confirmation panel
    - Test: submitting create form calls createTemplate and shows success message
    - Test: error state renders retry button
  </behavior>

  <action>
    **First, create `frontend-v2/src/__tests__/TemplatesPage.test.tsx`:**
    Follow the scaffold.test.tsx pattern for mocking. Use `vi.mock` to mock `useTemplates` hook, controlling `templates`, `isLoading`, `error`, and mutation functions. Tests must cover:
    - Loading state: renders spinner with "Loading templates..."
    - Empty state: "No chore templates yet" heading with "Create Template" button
    - Populated state: renders template rows with title, category, points, and edit/delete icons
    - Create form: clicking "Create Template" opens form; submitting calls createTemplate
    - Edit form: clicking edit icon shows form pre-filled with template data
    - Delete confirmation: clicking delete icon shows inline ConfirmDelete panel
    - Error state: renders "Try again" retry button
    - Success message: green banner visible after create/edit, auto-dismisses
    - Success message cleans up setTimeout on unmount (useEffect cleanup pattern)
    - Form submit via explicit button `onClick` handler, not relying on form `onSubmit` + label `htmlFor` associations

    Run `cd frontend-v2 && npx vitest run -- src/__tests__/TemplatesPage.test.tsx` — tests must FAIL (RED gate, implementation not yet written).

    **Then, build TemplatesPage per UI-SPEC Page 1 contract exactly.** Follow LoginPage useState + form pattern.

    **Imports:** `{ useState, useEffect }` from react, `{ useTemplates }` from hooks, `{ NavBar, ConfirmDelete }` from components, `{ Plus, Pencil, Trash2 }` from lucide-react.

    **State management:**
    - `showForm` (boolean) — whether create/edit form is visible.
    - `editingTemplate` (Template | null) — template being edited (null = create mode).
    - `deletingTemplateId` (number | null) — which template's delete confirmation is open.
    - `successMessage` (string | null) — green success banner, auto-dismissed via 3s `setTimeout` with `useEffect` cleanup to prevent React warning on unmount.
    - Form fields: `title`, `description`, `points`, `category` — all `useState`.
    - Form error: `formError` (string | null).

    **Loading state:** `<div className="min-h-screen bg-gray-50"><NavBar /><div className="flex items-center justify-center py-12"><div className="animate-spin rounded-full h-6 w-6 border-b-2 border-primary"></div><span className="ml-3 text-gray-500">Loading templates...</span></div></div>` (UI-SPEC §9 Inline loading).

    **Error state:** Centered error panel with "Something went wrong" heading, "Unable to load templates. Check your connection and try again." body, and "Try again" button that calls `window.location.reload()`. Pattern from ProtectedRoute.tsx lines 28-40 (UI-SPEC §10).

    **Empty state:** Centered text block: "No chore templates yet" (bold), "Create your first chore template to start assigning tasks." (gray), "Create Template" primary button below. (UI-SPEC §11 + copywriting).

    **Populated state:**
    - Page header: `<h2 className="text-2xl font-bold text-gray-900 mb-4">Chore Templates</h2>`.
    - Success message (if any): `<div className="bg-green-50 text-green-700 px-4 py-2 rounded-lg mb-4">Template saved!</div>` (UI-SPEC §12).
    - Create button: `<button onClick={() => { setShowForm(true); setEditingTemplate(null); resetForm(); }} className="bg-primary text-white px-4 py-2 rounded-lg hover:bg-primary-hover mb-4 flex items-center gap-1"><Plus className="h-4 w-4" /> Create Template</button>`.
    - Table: CSS grid with `grid-cols-3` per UI-SPEC §6: title+category, points, actions.
      - Header row: `bg-gray-50 px-4 py-3 border-b text-sm font-normal text-gray-500`.
      - Data rows: `px-4 py-3 grid grid-cols-3 gap-4 items-center hover:bg-gray-50`.
      - Title column: template title (bold) + category below in `text-sm text-gray-500`.
      - Points column: `{template.points} pts`.
      - Actions column: edit icon `<Pencil className="h-4 w-4" />` with `aria-label="Edit template"`, delete icon `<Trash2 className="h-4 w-4" />` with `aria-label="Delete template"`. Icon buttons: `p-1 text-gray-400 hover:text-gray-600` (UI-SPEC §4 icon-only).

    **Create/Edit form** (appears above table when `showForm` is true):
    - Form card: `<div className="bg-white rounded-lg shadow-md p-6 mb-4">` (UI-SPEC §2 form card).
    - Fields: Title (required, text input), Description (optional, textarea rows=3), Points (required, number input, min=1), Category (required, text input — D-04: free-text, no dropdown).
    - All inputs: `w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-primary-ring` (UI-SPEC §3).
    - Labels: `block text-sm font-normal text-gray-700 mb-1` (UI-SPEC Typography: font-normal).
    - Buttons row: `<div className="flex gap-2 mt-4">`.
      - Save: `<button type="submit" disabled={isCreating || isUpdating} className="bg-primary text-white px-4 py-2 rounded-lg hover:bg-primary-hover disabled:opacity-50">{isCreating || isUpdating ? 'Saving...' : 'Save Template'}</button>`.
      - Cancel: `<button type="button" onClick={cancelForm} disabled={isCreating || isUpdating} className="bg-white border border-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-50 disabled:opacity-50">Discard changes</button>`.
    - Form error: `<div className="bg-red-50 text-red-600 p-3 rounded mb-4">{formError}</div>` (from LoginPage pattern).
    - On submit: call `createTemplate(data)` or `updateTemplate(editingTemplate.id, data)`. On success: close form, show success message, auto-dismiss after 3s. On error: show `formError`.
    - Pre-fill form when `editingTemplate` is set.

    **Delete flow** (UI-SPEC §Delete Flow):
    - Click delete icon → set `deletingTemplateId = template.id`.
    - Render `<ConfirmDelete>` below that row with:
      - `message="This will permanently delete the chore template and all its assignments. This cannot be undone. Continue?"` (UI-SPEC copywriting: delete template confirmation).
      - `deleteLabel="Delete Template"`, `keepLabel="Keep Template"`.
      - `onDelete`: call `deleteTemplate(id)`, on success show "Deleted." success message, clear `deletingTemplateId`.
      - `onCancel`: clear `deletingTemplateId`.
      - `isDeleting`: from hook's `isDeleting`.

    **Typography/Color rules** (UI-SPEC):
    - Page title: `text-2xl font-bold text-gray-900`.
    - Body text: `text-gray-600`.
    - Labels: `text-sm font-normal text-gray-700`.
    - No `font-medium` or `font-semibold` anywhere.
    - Primary buttons: `bg-primary text-white hover:bg-primary-hover`.
    - Destructive: red-600 for hover on delete icons.
  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit src/pages/TemplatesPage.tsx && npx vitest run -- src/__tests__/TemplatesPage.test.tsx -- no TypeScript errors, tests pass</automated>
  </verify>

  <acceptance_criteria>
    - TemplatesPage.test.tsx exists with 8 test cases covering all states (loading, empty, populated, create, edit, delete, error, success)
    - All 8 TemplatesPage tests pass
    - TemplatesPage renders NavBar at top
    - Loading state: spinner + "Loading templates..." text
    - Empty state: "No chore templates yet" + "Create Template" button
    - Populated state: grid table with title+category, points, edit/delete icons per row
    - Create form: title, description textarea, points number, category text inputs
    - Edit form: pre-filled with existing template data
    - Delete: inline red confirmation panel with "Delete Template" and "Keep Template" buttons
    - Success: green message auto-dismisses after 3s
    - Error state: "Unable to load templates" + retry button
    - All copywriting matches UI-SPEC exactly (no emoji, correct casing)
    - No font-medium usage — font-normal only for labels/body
    - All icon-only buttons have aria-label
    - TypeScript compilation: no errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MyChoresPage with filter and complete flow</name>
  <files>frontend-v2/src/pages/MyChoresPage.tsx, frontend-v2/src/__tests__/MyChoresPage.test.tsx</files>

  <read_first>frontend-v2/src/pages/TemplatesPage.tsx, frontend-v2/src/hooks/useAssignments.tsx, frontend-v2/src/components/FilterBar.tsx, frontend-v2/src/components/StatusBadge.tsx</read_first>

  <behavior>
    - Test: renders loading spinner
    - Test: renders empty state ("No chores assigned yet")
    - Test: renders chore list with name, due date, status badge, points, complete button
    - Test: status filter changes update visible chores (frontend-side filtering, D-09)
    - Test: date range filter limits visible chores
    - Test: clicking "Mark Complete" changes button to "Completing..." then row updates
    - Test: completed chores show green status badge, no complete button
    - Test: error state with retry
  </behavior>

  <action>
    **First, create `frontend-v2/src/__tests__/MyChoresPage.test.tsx`:**
    Follow the scaffold.test.tsx pattern for mocking. Use `vi.mock` to mock `useAssignments` and `useAuth` hooks. Tests must cover:
    - Loading state: renders spinner with "Loading your chores..."
    - Empty state: "No chores assigned yet" heading
    - Populated state: renders chore rows with name, due date, status badge, points, and "Mark Complete" button for PENDING
    - Status filter: changing filter updates visible chores (frontend-side filtering, D-09)
    - Date range filter: changing dates limits visible chores
    - Complete flow: clicking "Mark Complete" shows "Completing..." disabled state, then row refreshes
    - Completed chores: show green status badge, no complete button
    - Empty filter results: "No assignments match your filters" message with clear link
    - Error state: renders retry button

    Run `cd frontend-v2 && npx vitest run -- src/__tests__/MyChoresPage.test.tsx` — tests must FAIL (RED gate, implementation not yet written).

    **Then, build MyChoresPage per UI-SPEC Page 3 contract.** No edit/delete actions — only "Mark Complete".

    **Imports:** `{ useState, useMemo, useEffect }` from react, `{ useAssignments }` from hooks, `{ useAuth }` from hooks, `{ NavBar, FilterBar, StatusBadge }` from components, `{ CheckCircle2 }` from lucide-react.

    **Filter state (D-09: frontend-side, live, no submit button):**
    - `statusFilter` (string, default `'all'`): all/pending/completed.
    - `dateFrom` (string, default: first day of current month per D-11).
    - `dateTo` (string, default: last day of current month per D-11).
    - Default date calculation: use `useState(() => { const now = new Date(); ... })` lazy initializer.

    **Frontend-side filtering (D-09):**
    ```typescript
    const filtered = useMemo(() => {
      return assignments.filter(a => {
        if (statusFilter !== 'all' && a.status !== statusFilter.toUpperCase()) return false
        if (dateFrom && a.dueDate < dateFrom) return false
        if (dateTo && a.dueDate > dateTo) return false
        return true
      })
    }, [assignments, statusFilter, dateFrom, dateTo])
    ```

    **Clear filters:** Reset statusFilter to 'all', dateFrom to '', dateTo to ''.

    **Success state:** `successMessage` state, auto-dismiss via 3s `setTimeout` with `useEffect` cleanup to prevent React "Can't perform a React state update on an unmounted component" warning.

    **Layout:**
    - Outer: `<div className="min-h-screen bg-gray-50"><NavBar />`.
    - Main: `<main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">`.
    - Title: `<h2 className="text-2xl font-bold text-gray-900 mb-4">My Chores</h2>`.

    **Loading:** Inline spinner with "Loading your chores..." (UI-SPEC §9).

    **Error:** Pattern from TemplatesPage: "Something went wrong" + "Unable to load your chores. Check your connection and try again." + retry button.

    **Empty:** "No chores assigned yet" (bold heading), "A parent needs to assign a chore before it appears here." (body). No CTA button — child cannot create assignments.

    **Populated:**
    - FilterBar (no user filter needed — this is "my" chores only): `<FilterBar statusFilter={...} dateFrom={...} dateTo={...} onClear={clearFilters} showUserFilter={false} />`.
    - Success: `<div className="bg-green-50 text-green-700 px-4 py-2 rounded-lg mb-4 flex items-center justify-between"><span>Chore marked complete!</span></div>`.
    - Empty filter results: `<div className="text-center py-8 text-gray-500">No assignments match your filters. <button onClick={clearFilters} className="text-primary hover:underline">Clear filters</button></div>`.
    - Table: grid-cols-4 (chore name, due date, status+points, complete button). Per UI-SPEC §6 My Chores variant.
      - Chore name: `{assignment.template.title}` (bold) + category below in `text-sm text-gray-500`.
      - Due date: format as `MMM D, YYYY`. Overdue detection: if `new Date(assignment.dueDate) < new Date()` and status is PENDING → `text-red-600 font-bold`. Due today: show "Today" label.
      - Status badge + points: `<StatusBadge status={assignment.status} overdue={isOverdue} />` + `<span className="text-sm">{assignment.template.points} pts</span>`.
      - Complete button (only for PENDING): `<button onClick={() => handleComplete(assignment.id)} disabled={isCompleting} className="bg-primary text-white text-sm px-3 py-1 rounded-lg hover:bg-primary-hover disabled:opacity-50">{isCompleting ? 'Completing...' : 'Mark Complete'}</button>`.
      - Completed rows: no button, green status badge. Optional: `bg-green-50` row tint for completed.

    **Complete flow** (UI-SPEC §Complete Flow):
    - `handleComplete(id)`: call `completeAssignment(id)`. Button goes to "Completing..." disabled. On success: show "Chore marked complete!" success banner, row updates on next render (query invalidation). On error: show error text inline.

    **Typography:** Page title `text-2xl font-bold`, body `text-gray-600`, labels `text-sm font-normal`. No font-medium.
  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit src/pages/MyChoresPage.tsx && npx vitest run -- src/__tests__/MyChoresPage.test.tsx -- no TypeScript errors, tests pass</automated>
  </verify>

  <acceptance_criteria>
    - MyChoresPage.test.tsx exists with 9 test cases covering all states and complete flow
    - All 9 MyChoresPage tests pass
    - Loading state: spinner with "Loading your chores..."
    - Empty state: "No chores assigned yet" + no CTA button
    - FilterBar with status select and date range; live filtering (no submit)
    - Default filter: current month date range (D-11)
    - PENDING chores show "Mark Complete" button
    - COMPLETED chores show green status badge, no button
    - Complete flow: button → "Completing..." disabled → row refreshes with completed status
    - Success message: "Chore marked complete!" auto-dismisses after 3s
    - Error state with retry
    - TypeScript compilation: no errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (templates) | POST/PUT/DELETE requests sent via useTemplates hook; backend enforces PARENT-only |
| client→API (complete) | POST /api/assignments/:id/complete triggered by Mark Complete button; backend enforces ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-12 | Elevation of Privilege | CHILD renders TemplatesPage URL directly | mitigate | `<ProtectedRoute requiredRole="PARENT">` wrapper in App.tsx (Plan 03-07) redirects CHILD to dashboard; component itself has no role check (defense at route level) |
| T-3-13 | Tampering | Double-click "Mark Complete" sends duplicate requests | accept | Button disables immediately (`disabled={isCompleting}`); backend returns 409 on already-completed (idempotent). No practical exploit beyond wasted API call |
</threat_model>

<verification>
```bash
cd frontend-v2 && npx tsc --noEmit
```
No TypeScript errors in TemplatesPage.tsx or MyChoresPage.tsx. Tests for these pages are created as Wave 0 scaffold files in this task — run them after implementation:
```bash
cd frontend-v2 && npx vitest run -- src/__tests__/TemplatesPage.test.tsx src/__tests__/MyChoresPage.test.tsx
```
</verification>

<success_criteria>
- TemplatesPage: all states covered (loading, empty, error, populated, creating, editing, deleting)
- MyChoresPage: all states covered (loading, empty, error, populated, filtering, completing)
- Frontend-side filtering works: no query params, instant update
- Default date filter: current month (D-11)
- Complete flow: "Mark Complete" → "Completing..." → green row refresh
- All copywriting matches UI-SPEC
- UI-SPEC Typography rules: font-normal for labels/body, font-bold for headings. No font-medium
- TypeScript compilation: no errors
</success_criteria>

<output>
Create `.planning/phases/03-core-chore-crud/03-06-SUMMARY.md` when done
</output>
