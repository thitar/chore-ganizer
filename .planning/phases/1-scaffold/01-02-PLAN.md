---
phase: 1-scaffold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-v2/package.json
  - frontend-v2/tsconfig.json
  - frontend-v2/tsconfig.node.json
  - frontend-v2/vite.config.ts
  - frontend-v2/tailwind.config.js
  - frontend-v2/postcss.config.js
  - frontend-v2/index.html
  - frontend-v2/src/vite-env.d.ts
  - frontend-v2/src/main.tsx
  - frontend-v2/src/App.tsx
  - frontend-v2/src/index.css
  - frontend-v2/src/__tests__/scaffold.test.tsx
  - frontend-v2/src/test/setup.ts
autonomous: true
requirements: ["infrastructure"]
must_haves:
  truths:
    - "Developer runs `cd frontend-v2 && npm install && npm test` — all tests pass"
    - "Developer runs `npm run dev` — Vite dev server starts on port 5173"
    - "Browser opens http://localhost:5173 — React app renders with Tailwind-styled shell"
    - "Scaffold test verifies React component renders without crashing"
  artifacts:
    - path: "frontend-v2/package.json"
      provides: "Dependency manifest with 6 production deps and dev tooling"
      min_lines: 25
    - path: "frontend-v2/vite.config.ts"
      provides: "Vite build config + Vitest test config with jsdom environment"
      min_lines: 20
    - path: "frontend-v2/src/App.tsx"
      provides: "Root React component with Tailwind-styled app shell"
      min_lines: 10
    - path: "frontend-v2/src/__tests__/scaffold.test.tsx"
      provides: "Basic render test for the app shell"
      exports: ["scaffold test suite"]
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "<script type=\"module\" src=\"/src/main.tsx\">"
      pattern: "src.*main"
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "import App from './App'"
      pattern: "import.*App"
    - from: "vite.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles"
      pattern: "setupFiles"
---

<objective>
Create the `frontend-v2/` project from scratch: project config, Vite + React + Tailwind setup, minimal app shell with a styled welcome page, and scaffold render tests.

Purpose: Establish the entire frontend scaffold so that `cd frontend-v2 && npm install && npm test && npm run dev` produces a working Vite dev server rendering the app shell. Phase 2 (Authentication) will add the login page and routing.

Output: A fully functional frontend-v2/ project with configs, Tailwind CSS, React entry point, app shell, and passing scaffold tests.
</objective>

<execution_context>
@/home/thitar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/thitar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REWRITE.MD

Architecture target (from REWRITE.MD):
```
frontend-v2/
├── src/
│   ├── main.tsx              # React entry: render <App /> into #root
│   ├── App.tsx               # Root component (minimal shell for Phase 1)
│   ├── index.css             # Tailwind directives
│   ├── vite-env.d.ts         # Vite client type reference
│   ├── __tests__/
│   │   └── scaffold.test.tsx # Basic render test
│   └── test/
│       └── setup.ts          # Vitest setup (jsdom matchers)
├── index.html                # HTML entry with Vite script tag
├── package.json              # 6 production deps only
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── tailwind.config.js
└── postcss.config.js
```

Key decisions (from PROJECT.md Key Decisions):
- Rewrite in frontend-v2/ alongside existing app
- No CSRF — no token injection interceptor in the API client
- No Zod on the frontend for the rewrite (use plain TypeScript types)
- No zustand — auth state uses React context only
- No recharts, no sonner (removed deps per REWRITE.MD)

Frontend production dependencies (6 total, from REWRITE.MD):
- react, react-dom, react-router-dom
- axios
- @tanstack/react-query
- lucide-react
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project configuration files</name>
  <files>
    frontend-v2/package.json
    frontend-v2/tsconfig.json
    frontend-v2/tsconfig.node.json
    frontend-v2/vite.config.ts
    frontend-v2/tailwind.config.js
    frontend-v2/postcss.config.js
    frontend-v2/index.html
  </files>
  <action>
    Create the `frontend-v2/` directory and all configuration files.

    **`frontend-v2/package.json`**:
    ```json
    {
      "name": "chore-ganizer-frontend-v2",
      "private": true,
      "version": "2.1.10",
      "type": "module",
      "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "test": "vitest run",
        "test:watch": "vitest"
      },
      "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-router-dom": "^6.22.0",
        "axios": "^1.13.6",
        "@tanstack/react-query": "^5.95.2",
        "lucide-react": "^0.577.0"
      },
      "devDependencies": {
        "typescript": "^5.3.3",
        "vite": "^6.2.0",
        "@vitejs/plugin-react": "^4.2.1",
        "vitest": "^4.1.0",
        "@testing-library/react": "^16.3.2",
        "@testing-library/jest-dom": "^6.1.5",
        "@testing-library/user-event": "^14.5.1",
        "jsdom": "^23.0.1",
        "@types/react": "^18.2.0",
        "@types/react-dom": "^18.2.18",
        "tailwindcss": "^3.4.0",
        "postcss": "^8.5.8",
        "autoprefixer": "^10.4.27"
      }
    }
    ```

    **`frontend-v2/tsconfig.json`**:
    ```json
    {
      "compilerOptions": {
        "target": "ES2020",
        "useDefineForClassFields": true,
        "lib": ["ES2020", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "skipLibCheck": true,
        "moduleResolution": "bundler",
        "allowImportingTsExtensions": true,
        "resolveJsonModule": true,
        "isolatedModules": true,
        "noEmit": true,
        "jsx": "react-jsx",
        "strict": true,
        "noUnusedLocals": false,
        "noUnusedParameters": false,
        "noFallthroughCasesInSwitch": true,
        "types": ["vitest/globals", "@testing-library/jest-dom"]
      },
      "include": ["src"],
      "references": [{ "path": "./tsconfig.node.json" }]
    }
    ```

    **`frontend-v2/tsconfig.node.json`**:
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["ES2023"],
        "module": "ESNext",
        "skipLibCheck": true,
        "moduleResolution": "bundler",
        "allowImportingTsExtensions": true,
        "resolveJsonModule": true,
        "isolatedModules": true,
        "noEmit": true,
        "strict": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        "noFallthroughCasesInSwitch": true
      },
      "include": ["vite.config.ts"]
    }
    ```

    **`frontend-v2/vite.config.ts`**:
    ```typescript
    /// <reference types="vitest" />
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'

    // https://vitejs.dev/config/
    export default defineConfig({
      plugins: [react()],
      server: {
        port: 5173,
        host: true,
        proxy: {
          '/api': {
            target: 'http://localhost:3010',
            changeOrigin: true,
          },
        },
      },
      test: {
        globals: true,
        environment: 'jsdom',
        setupFiles: './src/test/setup.ts',
        css: true,
      },
    })
    ```
    Note: The proxy config forwards `/api/*` requests to the backend dev server (Phase 1 prerequisite).

    **`frontend-v2/tailwind.config.js`**:
    ```js
    /** @type {import('tailwindcss').Config} */
    export default {
      content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
      ],
      theme: {
        extend: {
          colors: {
            primary: {
              DEFAULT: '#4F46E5',
              hover: '#4338CA',
              light: '#EEF2FF',
              ring: '#6366F1',
            },
          },
          fontFamily: {
            sans: ['Inter', 'system-ui', 'sans-serif'],
          },
        },
      },
      plugins: [],
    }
    ```

    **`frontend-v2/postcss.config.js`**:
    ```js
    export default {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    }
    ```

    **`frontend-v2/index.html`**:
    ```html
    <!DOCTYPE html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Chore-Ganizer</title>
        <link rel="icon" type="image/svg+xml" href="/vite.svg" />
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    After writing all files, run `cd frontend-v2 && npm install`.
  </action>
  <verify>
    <automated>ls frontend-v2/package.json frontend-v2/tsconfig.json frontend-v2/vite.config.ts frontend-v2/index.html && cd frontend-v2 && ls node_modules/.package-lock.json 2>/dev/null && echo "node_modules exists"</automated>
  </verify>
  <done>
    frontend-v2/ directory exists with all 7 config files. `npm install` completed successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create React app shell, styles, test setup, and scaffold tests</name>
  <files>
    frontend-v2/src/vite-env.d.ts
    frontend-v2/src/main.tsx
    frontend-v2/src/App.tsx
    frontend-v2/src/index.css
    frontend-v2/src/test/setup.ts
    frontend-v2/src/__tests__/scaffold.test.tsx
  </files>
  <action>
    Create all source files for the minimal React app shell.

    **`frontend-v2/src/vite-env.d.ts`**:
    ```typescript
    /// <reference types="vite/client" />
    ```

    **`frontend-v2/src/main.tsx`**:
    Entry point that renders `<App />` into `#root`. Wrap in `<React.StrictMode>`.
    ```typescript
    import React from 'react'
    import ReactDOM from 'react-dom/client'
    import App from './App'
    import './index.css'

    ReactDOM.createRoot(document.getElementById('root')!).render(
      <React.StrictMode>
        <App />
      </React.StrictMode>
    )
    ```

    **`frontend-v2/src/App.tsx`**:
    Minimal app shell with a centered welcome message styled with Tailwind. This will be replaced in Phase 2 with router-based navigation, but for Phase 1 it gives a visual confirmation that React + Tailwind work together.
    ```typescript
    function App() {
      return (
        <div className="min-h-screen bg-gray-50 flex items-center justify-center">
          <div className="text-center">
            <h1 className="text-4xl font-bold text-gray-900 mb-2">
              Chore-Ganizer
            </h1>
            <p className="text-lg text-gray-600">
              Family chore management — simplified.
            </p>
            <p className="mt-8 text-sm text-gray-400">
              App shell ready · Phase 1 Scaffold
            </p>
          </div>
        </div>
      )
    }

    export default App
    ```

    **`frontend-v2/src/index.css`**:
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    ```

    **`frontend-v2/src/test/setup.ts`**:
    ```typescript
    import '@testing-library/jest-dom'
    ```

    **`frontend-v2/src/__tests__/scaffold.test.tsx`**:
    Basic render test verifying the app shell renders without crashing and shows expected content.
    ```typescript
    import { render, screen } from '@testing-library/react'
    import App from '../App'

    describe('App shell', () => {
      it('renders the app title', () => {
        render(<App />)
        expect(screen.getByText('Chore-Ganizer')).toBeInTheDocument()
      })

      it('renders the tagline', () => {
        render(<App />)
        expect(screen.getByText(/Family chore management/i)).toBeInTheDocument()
      })

      it('renders without crashing', () => {
        const { container } = render(<App />)
        expect(container.firstChild).toBeInTheDocument()
      })
    })
    ```

    After writing all files, run `cd frontend-v2 && npm test` to verify the scaffold tests pass.
  </action>
  <verify>
    <automated>cd frontend-v2 && npm test 2>&1</automated>
  </verify>
  <done>
    All 3 scaffold tests pass. `npm run dev` starts Vite dev server on port 5173. Browser renders the app shell with Tailwind-styled heading.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Vite dev server | Dev server serves source files via HTTP (local dev only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Information Disclosure | Vite dev server | accept | Dev-only; serves raw source in dev mode. No production deployment without build step. Production build outputs static files to dist/. |
| T-1-05 | Spoofing | Any component | accept | Phase 1 has no auth, no mutating features, no user-specific data. Auth is Phase 2. |
</threat_model>

<verification>
1. `cd frontend-v2 && npm test` — 3 scaffold tests pass
2. `cd frontend-v2 && npm run dev` — Vite starts on 5173 without errors
3. Open `http://localhost:5173` — browser shows "Chore-Ganizer" heading with "Family chore management — simplified." tagline
</verification>

<success_criteria>
- frontend-v2/ directory exists with all config files and source files
- `npm test` passes (3 tests: renders title, renders tagline, no crash)
- `npm run dev` starts Vite on port 5173
- Browser renders Tailwind-styled app shell with "Chore-Ganizer" heading
</success_criteria>

<output>
After completion, create `.planning/phases/1-scaffold/01-02-SUMMARY.md`
</output>
