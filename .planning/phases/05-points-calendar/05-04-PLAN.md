---
phase: 05-points-calendar
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - frontend-v2/src/pages/CalendarPage.tsx
  - frontend-v2/src/hooks/useCalendar.tsx
  - frontend-v2/src/api/calendar.api.ts
  - frontend-v2/src/components/NavBar.tsx
  - frontend-v2/src/App.tsx
  - frontend-v2/src/__tests__/CalendarPage.test.tsx
autonomous: true
requirements: [CAL-01, CAL-02, CAL-03]
---

<objective>
Calendar page frontend: API client (reuses /api/assignments with date filter), React Query hook, CalendarPage with monthly grid + color pills + prev/next navigation, NavBar link, App.tsx route.
</objective>

<context>
@.planning/phases/05-points-calendar/05-CONTEXT.md
@.planning/phases/05-points-calendar/05-UI-SPEC.md
@frontend-v2/src/api/assignments.api.ts
@frontend-v2/src/hooks/useAssignments.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar API + hook + page + NavBar + route</name>
  <files>frontend-v2/src/api/calendar.api.ts, frontend-v2/src/hooks/useCalendar.tsx, frontend-v2/src/pages/CalendarPage.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/App.tsx, frontend-v2/src/__tests__/CalendarPage.test.tsx</files>
  <read_first>frontend-v2/src/api/assignments.api.ts, frontend-v2/src/hooks/useAssignments.tsx</read_first>

  <action>
    1. Create `frontend-v2/src/api/calendar.api.ts`:
       - Reuses `/api/assignments` endpoint
       - `getCalendar(from, to): Promise<Assignment[]>` calls `assignmentsApi.getAll()` and filters by dueDate range
       - `getCalendarMonth(year, month): Promise<Assignment[]>` helper that computes from/to

    2. Create `frontend-v2/src/hooks/useCalendar.tsx`:
       - `useCalendarMonth(year, month)` returns `{ assignments, isLoading, error, prevMonth, nextMonth, goToToday }`

    3. Create `frontend-v2/src/pages/CalendarPage.tsx` per UI-SPEC:
       - Header with prev/next chevron buttons + month/year display
       - 7-column grid (Sun-Sat) × 6 rows
       - Each day cell shows date number + assignment pills
       - Color pills use assignedTo.color
       - Today highlighted
       - Legend with user color swatches
       - States: loading, error, empty month

    4. Update `NavBar.tsx`: Add "Calendar" link for all roles

    5. Update `App.tsx`: Add `/calendar` route

    6. Create `CalendarPage.test.tsx` with 6+ tests
  </action>

  <verify>
    <automated>cd frontend-v2 && npx vitest run src/__tests__/CalendarPage.test.tsx && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - CalendarPage shows month grid with day cells
    - Assignments render as color-coded pills
    - Prev/next navigation works
    - Today is highlighted
    - Legend shows user colors
    - All tests pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd frontend-v2 && npx vitest run` — all pass
- `cd frontend-v2 && npx tsc --noEmit` — no errors
</verification>

<success_criteria>
- Calendar API + hook + page + NavBar + route in place
- Color-coded assignment pills
- Month navigation works
- All tests pass
</success_criteria>
