---
phase: 05-points-calendar
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - frontend-v2/src/api/points.api.ts
  - frontend-v2/src/hooks/usePoints.tsx
  - frontend-v2/src/pages/PointsPage.tsx
  - frontend-v2/src/components/NavBar.tsx
  - frontend-v2/src/App.tsx
  - frontend-v2/src/__tests__/PointsPage.test.tsx
autonomous: true
requirements: [PTS-01, PTS-02, PTS-03, PTS-04]
---

<objective>
Frontend points: API client, React Query hook, PointsPage with balance + log + parent adjust form, NavBar link, App.tsx route.
</objective>

<context>
@.planning/phases/05-points-calendar/05-CONTEXT.md
@.planning/phases/05-points-calendar/05-UI-SPEC.md
@frontend-v2/src/api/recurring.api.ts
@frontend-v2/src/hooks/useRecurringChores.tsx
@frontend-v2/src/pages/RecurringChoresPage.tsx
@frontend-v2/src/components/NavBar.tsx
@frontend-v2/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API + hook + page + NavBar + route</name>
  <files>frontend-v2/src/api/points.api.ts, frontend-v2/src/hooks/usePoints.tsx, frontend-v2/src/pages/PointsPage.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/App.tsx, frontend-v2/src/__tests__/PointsPage.test.tsx</files>
  <read_first>frontend-v2/src/api/recurring.api.ts, frontend-v2/src/hooks/useRecurringChores.tsx, frontend-v2/src/pages/RecurringChoresPage.tsx</read_first>

  <action>
    1. Create `frontend-v2/src/api/points.api.ts`:
       - Axios instance at `/api/points`
       - `PointLog` interface with type, amount, reason, user
       - `getMyPoints(): Promise<{ user, balance, logs }>`
       - `getUserPoints(userId): Promise<{ user, balance, logs }>`
       - `adjustPoints(userId, amount, reason): Promise<PointLog>`

    2. Create `frontend-v2/src/hooks/usePoints.tsx`:
       - `useMyPoints()` returns `{ data, isLoading, error, refetch }`
       - `useAdjustPoints()` returns `{ mutate, isPending }`
       - `useAllUsersPoints()` for parent to view any family member

    3. Create `frontend-v2/src/pages/PointsPage.tsx` per UI-SPEC:
       - Balance card (prominent)
       - Parent adjust form (select user, amount, reason)
       - Log table with type badges
       - States: loading, error, empty, success toast

    4. Update `frontend-v2/src/components/NavBar.tsx`: Add "Points" link for all roles

    5. Update `frontend-v2/src/App.tsx`: Add `/points` route

    6. Create `frontend-v2/src/__tests__/PointsPage.test.tsx` with 6+ tests
  </action>

  <verify>
    <automated>cd frontend-v2 && npx vitest run src/__tests__/PointsPage.test.tsx && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - points.api.ts exports PointLog, getMyPoints, getUserPoints, adjustPoints
    - usePoints returns query + mutation
    - PointsPage shows balance, log, adjust form (parent only)
    - NavBar has "Points" link for all roles
    - App.tsx has /points route
    - All tests pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd frontend-v2 && npx vitest run` — all pass
- `cd frontend-v2 && npx tsc --noEmit` — no errors
</verification>

<success_criteria>
- API + hook + page + NavBar + route all in place
- Parent adjust form works
- Log table shows all types
- Tests pass
</success_criteria>
