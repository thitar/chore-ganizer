---
phase: 05-points-calendar
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend-v2/prisma/seed.ts
autonomous: true
requirements: [PTS-01, PTS-02]
must_haves:
  truths:
    - "PointLog type field supports EARNED, BONUS, DEDUCTION, PENALTY, REVERSED, ADJUSTMENT"
    - "Seed creates sample point logs for testing"
  artifacts:
    - path: "backend-v2/prisma/seed.ts"
      provides: "Seed with diverse point log types"
autonomous: true
---

<objective>
Update the seed script to add 2 more PointLog entries (BONUS, ADJUSTMENT) for testing. No schema changes needed — `type` is a free string.
</objective>

<context>
@.planning/phases/05-points-calendar/05-CONTEXT.md
@backend-v2/prisma/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BONUS and ADJUSTMENT seed entries</name>
  <files>backend-v2/prisma/seed.ts</files>
  <read_first>backend-v2/prisma/seed.ts</read_first>

  <action>
    In seed.ts, after the existing `prisma.pointLog.createMany` call, the existing 2 logs (EARNED) are there. Add 2 more:
    - BONUS for Alice: `{ userId: alice.id, amount: 15, type: 'BONUS', reason: 'Great week of chores!' }`
    - ADJUSTMENT for Bob: `{ userId: bob.id, amount: -5, type: 'ADJUSTMENT', reason: 'Snack correction' }`

    Update the console.log to reflect total: `'Seed complete: 4 users, 4 chore templates, 4 point logs, 2 recurring chores'`
  </action>

  <verify>
    <automated>cd backend-v2 && DATABASE_URL="file:./dev.db" npx prisma db push --accept-data-loss && DATABASE_URL="file:./dev.db" npx prisma db seed</automated>
  </verify>

  <acceptance_criteria>
    - Seed runs without error
    - console.log shows "4 point logs"
    - Prisma pointLog table has 4 rows
  </acceptance_criteria>
</task>

</tasks>

<verification>
After task: `cd backend-v2 && npx prisma db seed` succeeds, console shows 4 point logs.
</verification>

<success_criteria>
- Seed updated to add BONUS and ADJUSTMENT entries
- No schema changes
- All existing tests still pass
</success_criteria>
