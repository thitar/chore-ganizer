---
phase: 12-chore-due-soon-lazy-trigger
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/services/notification.service.ts
  - backend/src/services/assignment.service.ts
  - backend/src/__tests__/services/assignment.service.test.ts
  - backend/src/__tests__/services/notification.service.test.ts
autonomous: true
requirements:
  - NOTIFY-03
  - NOTIFY-07

must_haves:
  truths:
    - "When a user opens the app with an un-notified PENDING assignment due today for someone with an ntfy topic, a fetch POST is sent to {NTFY_BASE_URL}/{topic} with body '{title} — {N} pts, due today', priority 4, tags warning+alarm_clock, and click /chores/{id}"
    - "When a due-today item has already been notified (dueNotifiedAt is set), no additional fetch fires — deduped via dueNotifiedAt"
    - "When NTFY_BASE_URL is unset or the assigned user has no ntfyTopic, no fetch fires and getAll still returns items"
    - "When the ntfy server is unreachable (fetch throws), getAll still returns items successfully and dueNotifiedAt is NOT written (will retry on next view)"
    - "Concurrent fetches from two devices opening the app simultaneously each attempt updateMany with WHERE dueNotifiedAt IS NULL — only one wins"
    - "sendNtfy returns a boolean indicating whether the HTTP fetch completed without throwing, so the sweep can conditionally write dueNotifiedAt"
  artifacts:
    - backend/src/services/notification.service.ts — sendNtfy returns Promise<boolean>, notifyChoreDueSoon is async returning Promise<boolean>, AssignmentWithIncludes extended with dueNotifiedAt and status
    - backend/src/services/assignment.service.ts — Prisma queries include ntfyTopic and dueNotifiedAt; new exported notifyDueSoon() sweep function; getAll() calls sweep after building combined sorted list
    - backend/src/__tests__/services/assignment.service.test.ts — Prisma mock extended with updateMany on choreAssignment and recurringOccurrence; getAll test data updated with dueNotifiedAt and ntfyTopic; 7 new sweep notification test cases
    - backend/src/__tests__/services/notification.service.test.ts — sendNtfy tests updated for boolean return; notifyChoreDueSoon tests updated for async
  key_links:
    - getAll() -> notifyDueSoon() (lazy sweep, called after combined list is sorted, per D-06)
    - notifyDueSoon() -> sendNtfy() (awaited for return value, per D-07)
    - sendNtfy() returns true -> updateMany(where: { id, dueNotifiedAt: null }, per D-08)
    - updateMany atomic WHERE = concurrent dedup (per D-09)
---

<objective>
Implement the lazy "chore-due-soon" notification sweep that piggybacks on assignment.service.getAll() — when a user fetches their chore list, the system checks for due-today items that haven't been notified yet, fires push notifications, and records dueNotifiedAt to prevent re-notification.

Purpose: This completes NOTIFY-03 (due-soon push with priority 4, tags warning+alarm_clock) and NOTIFY-07 (lazy trigger on assignment fetch, no cron). Without this, users never get reminded when chores are due today — they must check the app manually.

Output: Modified notification.service.ts (sendNtfy returns boolean, notifyChoreDueSoon async, type extension) + modified assignment.service.ts (sweep function, getAll wiring, expanded Prisma includes) + updated tests for both files.
</objective>

<execution_context>
@/home/thitar/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/thitar/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-chore-due-soon-lazy-trigger/12-CONTEXT.md
@backend/src/services/notification.service.ts
@backend/src/services/assignment.service.ts
@backend/src/services/notification.formatters.ts
@backend/src/services/recurring.service.ts
@backend/src/__tests__/services/assignment.service.test.ts
@backend/src/__tests__/services/notification.service.test.ts
@backend/src/config/notifications.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write sweep notification tests and update existing test data shapes</name>
  <files>
    backend/src/__tests__/services/assignment.service.test.ts
    backend/src/__tests__/services/notification.service.test.ts
  </files>
  <read_first>
    - backend/src/__tests__/services/assignment.service.test.ts (full file — mock setup, existing getAll tests at lines 226-281, ntfy-disabled describe at lines 170-224)
    - backend/src/__tests__/services/notification.service.test.ts (lines 16-22 — jest.spyOn pattern; lines 134-144 — existing notifyChoreDueSoon tests)
    - backend/src/services/notification.service.ts (lines 37-42 — current AssignmentWithIncludes type; lines 10-34 — sendNtfy with try/catch; lines 51-56 — notifyChoreDueSoon wrapper)
    - backend/src/services/assignment.service.ts (lines 62-83 — current Prisma queries; lines 85-117 — combined item maps; line 117 — insertion point for sweep call)
    - backend/src/config/notifications.ts (isNtfyConfigured export)
  </read_first>
  <behavior>
    - The sweep notification tests verify that calling `getAll()` with items in specific conditions results in `global.fetch` being called or not called, with correct payload verification
    - The sweep function filters combined items for: dueDate === today, status in [PENDING, PARTIALLY_COMPLETE], dueNotifiedAt === null, assignedTo.ntfyTopic !== null, and isNtfyConfigured === true
    - Existing getAll tests must have mock data updated to include `dueNotifiedAt: null` and `assignedTo.ntfyTopic`
    - Prisma mock must include `updateMany` on `choreAssignment` and `recurringOccurrence`
    - notification.service.test.ts: sendNtfy tests assert boolean return; notifyChoreDueSoon tests become async
  </behavior>
  <action>
    <!-- planner-discipline-allow: dueNotifiedAt -->

    **Part A — Update assignment.service.test.ts prisma mock (lines 1-31):**

    1. Add `updateMany: jest.fn()` to `prisma.choreAssignment` mock (between `update` and `delete`)
    2. Add `updateMany: jest.fn()` to `prisma.recurringOccurrence` mock (after `findMany`)
    3. Same additions in the ntfy-disabled test's doMock prisma definition (lines 177-195)

    **Part B — Update existing getAll tests (lines 226-281):**

    4. PARENT role test (line 228-242): add `dueNotifiedAt: null` as root field (after `createdAt`), and `ntfyTopic: 'alice-topic'` inside `assignedTo` (after `color`)
    5. Update assertion at line 250 to verify `result[0].dueNotifiedAt` is `null`
    6. CHILD role test (line 255-268): add same fields — `dueNotifiedAt: null`, `assignedTo.ntfyTopic: null`
    7. Update assertion at line 277 to verify `result[0].dueNotifiedAt` is `null`

    **Part C — Update ntfy-disabled test mock (lines 170-224):**

    8. Add `updateMany: jest.fn()` to the `choreAssignment` and `recurringOccurrence` entries inside the `jest.doMock` prisma shape (lines 184-193)

    **Part D — Add sweep notification tests (new describe block after line 281):**

    Add a new describe block `describe('assignmentService.getAll - notification sweep', () => { ... })` containing:

    - A `beforeAll` hook using `jest.useFakeTimers()` with `jest.setSystemTime(new Date('2026-07-02T12:00:00Z'))` so `new Date().toISOString().slice(0,10)` is `'2026-07-02'`
    - An `afterAll` hook calling `jest.useRealTimers()`
    - A `makeRegularItem(overrides = {}): any` helper returning a REGULAR-era Prisma shape with `dueDate: new Date('2026-07-02T00:00:00Z')`, `status: 'PENDING'`, `dueNotifiedAt: null`, `assignedTo: { id:3, name:'Alice', color:'#10B981', ntfyTopic:'alice-topic' }`, `template: { id:1, title:'Wash Dishes', points:10, category:'kitchen' }`
    - A `makeRecurringItem(overrides = {}): any` helper returning a RECURRING-era Prisma shape with same fields but wrapped in `chore: { id, choreTemplateId, template: {...} }` per the existing RecurringOccurrence structure (lines 100-115)

    Test 1 — fires ntfy fetch with correct payload for due-today un-notified item:
    - Mock `choreAssignment.findMany` with `[makeRegularItem()]`, `recurringOccurrence.findMany` with `[]`
    - `jest.spyOn(global, 'fetch').mockResolvedValue(new Response())`
    - Call `await assignmentService.getAll(1, 'PARENT')` + `await new Promise(process.nextTick)`
    - Assert `fetchSpy` called once with URL `'https://ntfy.example.com/alice-topic'`, method POST, body `'Wash Dishes - 10 pts, due today'`, headers `{ Title:'Chore-Ganizer', Priority:'4', Tags:'warning,alarm_clock', Click:'/chores/1' }`
    - Assert `prisma.choreAssignment.updateMany` called with `{ where: { id: 1, dueNotifiedAt: null }, data: { dueNotifiedAt: expect.any(Date) } }`
    - `fetchSpy.mockRestore()`

    Test 2 — does not fire for already-notified due-today item:
    - `makeRegularItem({ dueNotifiedAt: new Date('2026-07-02T10:00:00Z') })`
    - Assert `fetchSpy` NOT called, `prisma.choreAssignment.updateMany` NOT called

    Test 3 — does not fire for item due tomorrow:
    - `makeRegularItem({ dueDate: new Date('2026-07-03T00:00:00Z') })`
    - Assert `fetchSpy` NOT called

    Test 4 — does not fire when assignedTo.ntfyTopic is null:
    - `makeRegularItem({ assignedTo: { id:3, name:'Alice', color:'#10B981', ntfyTopic: null } })`
    - Assert `fetchSpy` NOT called

    Test 5 — does not fire for COMPLETED item:
    - `makeRegularItem({ status: 'COMPLETED' })`
    - Assert `fetchSpy` NOT called

    Test 6 — getAll still succeeds and dueNotifiedAt NOT written when fetch throws:
    - `jest.spyOn(global, 'fetch').mockRejectedValue(new Error('Network error'))`
    - Also spy on `console.warn` to verify the `[ntfy] send failed` log
    - Assert `result` has length 1 with `result[0].id === 1`
    - Assert `prisma.choreAssignment.updateMany` NOT called
    - Assert `consoleWarnSpy` called with `expect.stringContaining('[ntfy] send failed')`

    Test 7 — REGULAR + RECURRING both trigger notifications:
    - Mock both `choreAssignment.findMany` with `[makeRegularItem()]` and `recurringOccurrence.findMany` with `[makeRecurringItem()]`
    - Assert `fetchSpy` called 2 times
    - Assert `choreAssignment.updateMany` called with id:1, `recurringOccurrence.updateMany` called with id:10, both with `where: { id, dueNotifiedAt: null }`

    **Part E — Update notification.service.test.ts for boolean/async changes:**

    9. Add 3 new `sendNtfy` return-value tests after line 82 (before "sendNtfy - unconfigured"):
       - `returns true when fetch succeeds`: `await sendNtfy('test-topic','Title','body')` then `expect(result).toBe(true)`
       - `returns false when fetch throws`: mock fetch to reject, `const result = await sendNtfy(...)`, `expect(result).toBe(false)`
       - `returns false when topic is null`: `const result = await sendNtfy(null,...)`, `expect(result).toBe(false)`

    10. Update `notifyChoreDueSoon` tests (lines 134-144):
        - First test: rename to `'calls sendNtfy and returns true when topic is set'`, make `async`, add `const result = await notifyChoreDueSoon(mockAssignment)`, then `expect(result).toBe(true)` followed by existing fetch assertion
        - Second test: rename to `'returns false when topic is null'`, make `async`, add `const result = await notifyChoreDueSoon({...mockAssignment, assignedTo:{ntfyTopic:null}})`, then `expect(result).toBe(false)`, keep existing `expect(global.fetch).not.toHaveBeenCalled()`

    Decision references:
    - D-03: Status filter (PENDING, PARTIALLY_COMPLETE) — Test 5 covers COMPLETED exclusion
    - D-04: Today-only — Test 3 covers tomorrow exclusion
    - D-05: Enriched includes — mock data includes dueNotifiedAt and ntfyTopic
    - D-06: Separate sweep function — tested via getAll() which calls it
    - D-07: Write after send — Test 6 verifies updateMany NOT called when fetch throws
    - D-08: Conditional updateMany — Test 1 verifies where: { id, dueNotifiedAt: null }
    - D-09: No $transaction — no transaction assertions needed
  </action>
  <verify>
    <automated>cd backend && npx jest --testPathPattern="assignment.service|notification.service" --no-coverage 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Prisma mock has `updateMany: jest.fn()` on both `choreAssignment` and `recurringOccurrence` (including ntfy-disabled mock)
    - Existing getAll test data includes `dueNotifiedAt: null` and `assignedTo.ntfyTopic`
    - 7 sweep notification test cases exist inside `describe('assignmentService.getAll - notification sweep')`
    - notification.service tests include 3 sendNtfy boolean-return tests + 2 updated notifyChoreDueSoon async tests
    - `npm test -- --testPathPattern=assignment.service` shows sweep tests FAILING because notifyDueSoon does not exist yet
    - `npm test -- --testPathPattern=notification.service` shows at least the async tests FAILING because sendNtfy still returns void
    - Each test manages its own fetch spy with mockRestore()
  </acceptance_criteria>
  <done>All tests written and failing (RED phase). Existing test data updated. Prisma mock extended. No production code changed yet.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — Extend types, create sweep function, wire into getAll, make sendNtfy awaitable</name>
  <files>
    backend/src/services/notification.service.ts
    backend/src/services/assignment.service.ts
  </files>
  <read_first>
    - backend/src/services/notification.service.ts (full file — current sendNtfy at lines 10-34, AssignmentWithIncludes at lines 37-42, notifyChoreDueSoon at lines 51-56)
    - backend/src/services/assignment.service.ts (full file — current getAll at lines 38-118, existing import from notification.service at line 4)
    - backend/src/__tests__/services/assignment.service.test.ts (the tests written in Task 1 — understand expected behaviors)
    - backend/src/__tests__/services/notification.service.test.ts (the tests updated in Task 1 — understand boolean/async expectations)
    - backend/src/config/notifications.ts (isNtfyConfigured export)
  </read_first>
  <behavior>
    - `sendNtfy()` returns `Promise<boolean>`: `true` when the fetch call completed without throwing, `false` when fetch threw or no-op due to config/topic
    - `notifyChoreDueSoon()` becomes `async` returning `Promise<boolean>` — propagates sendNtfy's return value; existing callers using `void notifyChoreDueSoon(...)` continue to work because the `void` operator discards the promise
    - `AssignmentWithIncludes` type gains `dueNotifiedAt: Date | null` and `status: string` fields
    - `getAll()` expands Prisma includes: `assignedTo` select adds `ntfyTopic: true` in both queries; `dueNotifiedAt` is automatically returned by Prisma (include-based query returns all root-level scalar fields)
    - Combined item maps (regular/recurring) include `dueNotifiedAt: a.dueNotifiedAt?.toISOString() ?? null` and pass through `assignedTo.ntfyTopic` from the expanded select
    - New exported `notifyDueSoon()` function in assignment.service.ts filters for due-today un-notified items with topics, calls `sendNtfy` directly (awaited for return value), conditionally writes `dueNotifiedAt` via `updateMany`, returns `Set<number>` of notified IDs
    - `getAll()` calls `notifyDueSoon()` after the combined sorted list is built (line 117), before the return
  </behavior>
  <action>
    **Part A — Modify `backend/src/services/notification.service.ts`:**

    1. Change `sendNtfy` return type from `Promise<void>` to `Promise<boolean>`. Add `return true` after the fetch await, and `return false` in the catch block replacing the existing void-return:

    ```
    export async function sendNtfy(
      topic: string | null,
      title: string,
      body: string,
      opts: { priority?: 1 | 2 | 3 | 4 | 5; tags?: string[]; click?: string } = {}
    ): Promise<boolean> {
      if (!isNtfyConfigured || !topic) return false
      const { baseUrl } = getNtfyConfig()
      const url = `${baseUrl}/${encodeURIComponent(topic)}`
      const headers: Record<string, string> = {
        Title: title,
        Priority: String(opts.priority ?? 3),
      }
      if (opts.tags?.length) headers['Tags'] = opts.tags.join(',')
      if (opts.click) headers['Click'] = opts.click
      try {
        await fetch(url, { method: 'POST', body, headers, signal: AbortSignal.timeout(3000) })
        return true
      } catch (err) {
        console.warn(`[ntfy] send failed for topic ${topic}: ${err instanceof Error ? err.message : String(err)}`)
        return false
      }
    }
    ```

    2. Extend `AssignmentWithIncludes` type to add `dueNotifiedAt` and `status`:

    ```
    type AssignmentWithIncludes = {
      id: number
      template: { title: string; points: number }
      assignedTo: { ntfyTopic: string | null }
      dueDate: Date
      dueNotifiedAt: Date | null
      status: string
    }
    ```

    3. Change `notifyChoreDueSoon` from synchronous void to async returning Promise<boolean>:

    ```
    export async function notifyChoreDueSoon(assignment: AssignmentWithIncludes): Promise<boolean> {
      const topic = assignment.assignedTo.ntfyTopic
      if (!topic) return false
      const { title, body, priority, tags, click } = dueSoonBody(assignment)
      return sendNtfy(topic, title, body, { priority, tags, click })
    }
    ```

    **Part B — Modify `backend/src/services/assignment.service.ts`:**

    4. Update existing import at line 4 to also import `notifyChoreDueSoon` and `sendNtfy`:
       `import { notifyChoreAssigned, notifyChoreDueSoon, sendNtfy } from './notification.service'`

    5. Add import for `dueSoonBody` from formatters and `isNtfyConfigured` from config:
       `import { dueSoonBody } from './notification.formatters'`
       `import { isNtfyConfigured } from '../config/notifications'`

    6. Expand `assignedTo` relation select in `choreAssignment.findMany` (line 67):
       Change `color: true }` to `color: true, ntfyTopic: true }`

    7. Expand `assignedTo` relation select in `recurringOccurrence.findMany` (line 79):
       Same change: `color: true }` to `color: true, ntfyTopic: true }`

    8. Add `dueNotifiedAt` to the regular items map (after line 94, before `createdAt`):
       `dueNotifiedAt: a.dueNotifiedAt?.toISOString() ?? null,`

    9. Add `dueNotifiedAt` to the recurring items map (after line 108, before `pointsAwarded`):
       `dueNotifiedAt: o.dueNotifiedAt?.toISOString() ?? null,`

    10. Add exported sweep function before `isRecordNotFoundError`:

    ```
    export async function notifyDueSoon(
      items: Array<{
        id: number
        type: 'REGULAR' | 'RECURRING'
        dueDate: string
        status: string
        dueNotifiedAt: string | null
        assignedTo: { ntfyTopic: string | null } | null
        template: { title: string; points: number }
      }>
    ): Promise<Set<number>> {
      if (!isNtfyConfigured) return new Set()

      const today = new Date().toISOString().slice(0, 10)
      const dueItems = items.filter(
        (i) =>
          i.dueDate === today &&
          (i.status === 'PENDING' || i.status === 'PARTIALLY_COMPLETE') &&
          !i.dueNotifiedAt &&
          i.assignedTo?.ntfyTopic
      )

      if (dueItems.length === 0) return new Set()

      const notified = new Set<number>()

      for (const item of dueItems) {
        const topic = item.assignedTo!.ntfyTopic!
        const { title, body, priority, tags, click } = dueSoonBody({
          id: item.id,
          template: { title: item.template.title, points: item.template.points },
          dueDate: new Date(item.dueDate + 'T00:00:00'),
        })

        const ok = await sendNtfy(topic, title, body, { priority, tags, click })
        if (!ok) continue  // D-07: do NOT mark notified if send failed

        if (item.type === 'REGULAR') {
          await prisma.choreAssignment.updateMany({
            where: { id: item.id, dueNotifiedAt: null },
            data: { dueNotifiedAt: new Date() },
          })
        } else {
          await prisma.recurringOccurrence.updateMany({
            where: { id: item.id, dueNotifiedAt: null },
            data: { dueNotifiedAt: new Date() },
          })
        }

        notified.add(item.id)
      }

      return notified
    }
    ```

    11. Wire sweep call into getAll(). Replace the return statement at line 117:
    ```
    const combined = [...regular, ...recurring].sort((a, b) => a.dueDate.localeCompare(b.dueDate))
    void notifyDueSoon(combined)
    return combined
    ```

    Key design enforcement:
    - D-06: Sweep is a separately exported function, independently testable
    - D-07: `await sendNtfy(...)` returns boolean; `if (!ok) continue` skips updateMany when send fails
    - D-08: `updateMany({ where: { id: item.id, dueNotifiedAt: null }, ... })` is the concurrent-dedup
    - D-09: No $transaction wrapper — SQLite serializes updateMany, atomic conditional WHERE
    - D-10: Function returns `Set<number>` of IDs that were notified
  </action>
  <verify>
    <automated>cd backend && npx jest --testPathPattern="assignment.service|notification.service" --no-coverage 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - sendNtfy returns Promise&lt;boolean&gt; (true on successful fetch, false on throw or no-op)
    - notifyChoreDueSoon is async, returns Promise&lt;boolean&gt;, propagates sendNtfy's return
    - AssignmentWithIncludes type has dueNotifiedAt: Date | null and status: string
    - assignment.service.ts imports { notifyChoreDueSoon, sendNtfy, dueSoonBody, isNtfyConfigured }
    - getAll() includes ntfyTopic: true in both assignedTo selects
    - getAll() maps dueNotifiedAt in both regular and recurring maps
    - notifyDueSoon() exported function filters: dueDate===today, PENDING/PARTIALLY_COMPLETE, !dueNotifiedAt, has ntfyTopic
    - notifyDueSoon() calls await sendNtfy(), skips updateMany when send returns false
    - notifyDueSoon() uses updateMany with WHERE dueNotifiedAt: null for concurrent dedup
    - notifyDueSoon() returns Set&lt;number&gt; of notified IDs
    - getAll() calls `void notifyDueSoon(combined)` before returning
    - `npm test -- --testPathPattern=assignment.service` — ALL tests pass (GREEN phase)
    - `npm test -- --testPathPattern=notification.service` — ALL tests pass
    - `npm run lint` — no lint errors in modified files
    - `npm test` — full backend test suite passes with no regressions
  </acceptance_criteria>
  <done>sendNtfy returns boolean, notifyChoreDueSoon is async, AssignmentWithIncludes extended, notifyDueSoon function created in assignment.service.ts, getAll() enriched with ntfyTopic/dueNotifiedAt and calls sweep. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getAll() -> notifyDueSoon | Fire-and-forget side effect — notification failure must not affect chore list response |
| notifyDueSoon -> sendNtfy | Awaited for return value (boolean) — sendNtfy catches errors internally, never throws |
| sendNtfy -> ntfy HTTP server | External HTTP call — server may be unreachable; AbortSignal.timeout(3000) + internal catch + boolean return |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Denial of Service | notifyDueSoon call in getAll() | medium | mitigate | Fire-and-forget (void prefix) + sendNtfy internal catch + boolean return ensures notification failure never blocks or fails the chore list response. If ntfy unreachable, after the 3s timeout per item, getAll() returns normally |
| T-12-02 | Information Disclosure | ntfy topic in console log | low | accept | Topic is already stored in DB; log prefix is `[ntfy] send failed for topic {topic}` — already a Phase 9 pattern; server-side log, not exposed to client |
| T-12-03 | Race Condition | Two concurrent getAll() calls for same user | low | mitigate | Per-item updateMany with WHERE dueNotifiedAt:null is the concurrent-dedup mechanism (D-08, D-09). SQLite serializes writes, so only one call matches the row and writes the timestamp |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this phase — zero npm dependencies added |
</threat_model>

<verification>
- `npm test -- --testPathPattern=assignment.service` — all tests pass (7 new notification sweep + existing getAll/update/create/complete/delete/uncomplete)
- `npm test -- --testPathPattern=notification.service` — all tests pass (3 new boolean-return tests + 2 updated async tests)
- `npm run lint` — no lint errors in any modified file
- `npm test` — full backend test suite green (no regressions)
</verification>

<success_criteria>
1. `sendNtfy()` returns `Promise<boolean>` — callers can distinguish send success from send failure
2. `notifyChoreDueSoon()` is `async` returning `Promise<boolean>` — sweep function can conditionally write `dueNotifiedAt`
3. `AssignmentWithIncludes` type has `dueNotifiedAt: Date | null` and `status: string`
4. `getAll()` Prisma queries include `ntfyTopic: true` in both `assignedTo` selects; `dueNotifiedAt` maps through to combined items
5. `notifyDueSoon()` exists as exported function: filters due-today + PENDING/PARTIALLY_COMPLETE + un-notified + has-topic items, sends notification, writes `dueNotifiedAt` only on success, conditional WHERE prevents double-fire
6. `getAll()` calls `void notifyDueSoon(combined)` after building the sorted list
7. All 7 sweep notification tests pass: positive payload, already-notified, tomorrow, no-topic, COMPLETED, ntfy-unreachable, REGULAR+RECURRING
8. Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/12-chore-due-soon-lazy-trigger/12-01-SUMMARY.md` when done
</output>


