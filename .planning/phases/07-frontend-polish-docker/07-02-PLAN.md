---
phase: 7
slug: frontend-polish-docker
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - docker-compose.yml
  - backend-v2/Dockerfile
  - backend-v2/docker-entrypoint.sh
  - frontend-v2/Dockerfile
  - frontend-v2/docker-entrypoint.sh
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
---

<objective>
Update docker-compose.yml to point to backend-v2 and frontend-v2. Create Dockerfiles and entrypoint scripts for both.
</objective>

<context>
@AGENTS.md
@.planning/phases/07-frontend-polish-docker/07-CONTEXT.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docker-compose.yml</name>
  <files>docker-compose.yml</files>
    <read_first>docker-compose.yml, AGENTS.md</read_first>

    <action>
      Update the root docker-compose.yml:
      1. Change backend context to `./backend-v2`
      2. Change frontend context to `./frontend-v2`
      3. Keep service names (backend, frontend)
      4. Update port mappings if needed
      5. Update volumes for data persistence
      6. Update environment variables (SESSION_SECRET, APP_VERSION, DATA_DIR)
      7. Ensure healthcheck at /api/health
    </action>
    <verify>
      <automated>docker compose config 2>&1 | head -30</automated>
    </verify>
    <acceptance_criteria>
      - docker-compose.yml valid syntax
      - Backend uses backend-v2 Dockerfile
      - Frontend uses frontend-v2 Dockerfile
      - Healthcheck defined
    </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Backend Dockerfile and entrypoint</name>
    <files>backend-v2/Dockerfile, backend-v2/docker-entrypoint.sh</files>
      <read_first>backend-v2/Dockerfile, backend-v2/docker-entrypoint.sh</read_first>

      <action>
        Update or create:
        1. `backend-v2/Dockerfile` — multi-stage: deps → build → runtime
        2. `backend-v2/docker-entrypoint.sh` — root, PUID/PGID, prisma db push, seed if empty, drop to appuser, start node
      </action>
      <verify>
        <automated>docker build -f backend-v2/Dockerfile backend-v2/ --no-cache 2>&1 | tail -10</automated>
      </verify>
      <acceptance_criteria>
        - Backend Dockerfile builds successfully
        - Entrypoint runs prisma db push
        - Entrypoint seeds DB if empty
        - Entrypoint starts node
      </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 3: Frontend Dockerfile and entrypoint</name>
      <files>frontend-v2/Dockerfile, frontend-v2/docker-entrypoint.sh</files>
        <read_first>frontend-v2/Dockerfile, frontend-v2/docker-entrypoint.sh</read_first>

        <action>
          Update or create:
          1. `frontend-v2/Dockerfile` — multi-stage: deps → build → nginx
          2. `frontend-v2/docker-entrypoint.sh` — generate /usr/share/nginx/html/config.js from env, start nginx
        </action>
        <verify>
          <automated>docker build -f frontend-v2/Dockerfile frontend-v2/ --no-cache 2>&1 | tail -10</automated>
        </verify>
        <acceptance_criteria>
          - Frontend Dockerfile builds successfully
          - Entrypoint generates config.js
          - Entrypoint starts nginx
        </acceptance_criteria>
    </task>

  </tasks>

  <verification>
  - `docker compose config` valid
  - Both Dockerfiles build
  - Entrypoint scripts executable
  </verification>

  <success_criteria>
  - docker-compose.yml updated to v2 directories
  - Both Dockerfiles work
  - Entrypoints run prisma + start services
  - Single `docker compose up --build -d` starts full app
  </success_criteria>
