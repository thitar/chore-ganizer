---
phase: 7
slug: frontend-polish-docker
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend-v2/src/pages/*.tsx
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
---

<objective>
Audit and fix loading/error/empty states + mobile responsive layout for all pages.
</objective>

<context>
@.planning/phases/07-frontend-polish-docker/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all pages have loading/error/empty states</name>
  <files>frontend-v2/src/pages/*.tsx</files>
    <read_first>frontend-v2/src/pages/RecurringChoresPage.tsx, frontend-v2/src/pages/PointsPage.tsx, frontend-v2/src/pages/CalendarPage.tsx</read_first>

    <action>
      Audit each page and ensure:
      1. Loading state with spinner and descriptive text
      2. Error state with retry button
      3. Empty state with helpful message

      For pages missing any state, add it following the established pattern.

      Pages to audit:
      - LoginPage (no loading needed)
      - DashboardPage (verify)
      - MyChoresPage (verify empty state)
      - TemplatesPage (verify)
      - RecurringChoresPage (verify)
      - AssignmentsPage (verify)
      - PointsPage (verify)
      - CalendarPage (verify empty state)
      - UsersPage (verify)
      - ProfilePage (verify)
    </action>
    <verify>
      <automated>cd frontend-v2 && npx vitest run</automated>
    </verify>
    <acceptance_criteria>
      - All pages have loading state
      - All pages have error state with retry
      - All pages have appropriate empty state
    </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mobile responsive layout fixes</name>
  <files>frontend-v2/src/pages/*.tsx</files>
    <read_first>frontend-v2/src/pages/RecurringChoresPage.tsx</read_first>

    <action>
      For each page, verify the layout works on mobile (375px viewport):
      1. Forms: full-width inputs, stacked fields
      2. Tables: appropriate grid columns
      3. Buttons: tap-friendly (min 44x44px)
      4. Padding: use px-4 on mobile, scale up

      Adjust Tailwind classes as needed.
    </action>
    <verify>
      <automated>cd frontend-v2 && npx tsc --noEmit</automated>
    </verify>
    <acceptance_criteria>
      - No layout breaks on mobile
      - All interactive elements tappable
      - TypeScript clean
    </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd frontend-v2 && npx vitest run` — all pass
- `cd frontend-v2 && npx tsc --noEmit` — no errors
</verification>

<success_criteria>
- All pages polished
- Mobile responsive
- All existing tests pass
</success_criteria>
