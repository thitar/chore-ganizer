---
phase: 02-prisma-modernization
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/package.json
  - backend/package-lock.json
autonomous: true
requirements:
  - DEPS-03
must_haves:
  truths:
    - "Prisma and @prisma/client are upgraded to latest 6.x"
    - "All existing unit tests (241) pass with Prisma 6.x"
    - "All integration tests pass with Prisma 6.x"
    - "Client is regenerated and TypeScript compiles cleanly"
  artifacts:
    - path: "backend/package.json"
      provides: "Updated prisma and @prisma/client to ^6.x.x"
      contains: '"@prisma/client": "^6'
    - path: "backend/package-lock.json"
      provides: "Lockfile with resolved 6.x versions"
      contains: '"@prisma/client":'
  key_links:
    - from: "backend/package.json"
      to: "backend/package-lock.json"
      via: "npm install"
      pattern: "@prisma/client"
    - from: "backend/node_modules/.prisma/client"
      to: "TypeScript compilation"
      via: "prisma generate"
      pattern: "prisma generate"
---

<objective>
Upgrade Prisma ORM from 5.22 to the latest 6.x, regenerate the client, verify all tests pass.

Purpose: Unblock future Prisma improvements and maintain support. Prisma 6.x is the current stable major — remaining on 5.22 blocks security updates and feature improvements.
Output: Updated package.json/package-lock.json, regenerated Prisma client in node_modules.
</objective>

<execution_context>
@/home/thitar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/thitar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prisma-modernization/02-CONTEXT.md
@.planning/phases/02-prisma-modernization/02-RESEARCH.md
.planning/phases/02-prisma-modernization/02-01-PLAN.md
</context>

<interfaces>
<!-- Prisma 6.x key interfaces (no codebase changes needed for most APIs) -->

From the RESEARCH.md findings:
- No `NotFoundError` usage in codebase — confirmed via grep
- No `findUniqueOrThrow`/`findFirstOrThrow` usage — confirmed via grep
- No `Bytes` fields in schema — Buffer→Uint8Array change has zero impact
- No PostgreSQL — implicit m-n schema change has zero impact
- Node.js 18.18+ requirement — project uses node:25-slim in Docker, Node 20 in CI

The only 6.x API changes relevant to this project:
- `@prisma/client` exports no longer include `NotFoundError` (not used here)
- Minimum TypeScript 5.1+ (project uses 5.3+)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade Prisma packages to 6.x and regenerate client</name>
  <files>backend/package.json, backend/package-lock.json</files>
  <read_first>backend/package.json</read_first>
  <action>
    Per D-05 (sequential approach) and D-06 (fix forward on 6.x):
    Now that the `$use` middleware has been migrated to `$extends` in Plan 01, proceed with the Prisma 6.x upgrade.

    **Prerequisites:** Plan 01 must be complete — `$use` is fully eliminated from the codebase. Prisma 6.x removes `$use` entirely.

    **Steps:**

    1. Install Prisma 6.x packages:
    ```bash
    cd backend
    npm install @prisma/client@6
    npm install -D prisma@6
    ```

    2. Regenerate the Prisma client:
    ```bash
    cd backend
    npx prisma generate
    ```

    3. Verify TypeScript compilation:
    ```bash
    cd backend
    npx tsc --noEmit 2>&1 | head -50
    ```

    **Expected changes to package.json:**
    - `"@prisma/client": "^5.22.0"` → `"@prisma/client": "^6.x.x"` (latest 6.x)
    - `"prisma": "^5.7.1"` → `"prisma": "^6.x.x"` (latest 6.x)

    **Per D-06:** Fix forward on any 6.x test failures — pin at latest 6.x, fix compatibility issues, document breaking changes. Do NOT revert to 5.22.

    **Research indicates zero breaking changes for this project** — verified:
    - No `NotFoundError` usage in codebase
    - No `findUniqueOrThrow`/`findFirstOrThrow` in codebase
    - Schema uses SQLite, not PostgreSQL (implicit m-n schema change is pg-only)
    - No `Bytes` fields (Buffer→Uint8Array irrelevant)
    - TypeScript 5.3+ (meets 5.1 minimum)
    - Node.js 18+ (CI uses Node 20, Docker uses node:25-slim)
  </action>
  <acceptance_criteria>
    - `backend/package.json` contains `"@prisma/client": "^6` — confirms 6.x upgrade
    - `backend/package.json` contains `"prisma": "^6` — confirms 6.x CLI upgrade
    - `backend/package.json` does NOT contain `"@prisma/client": "^5` — confirms old version removed
    - `npx prisma --version` output includes "6." — confirms CLI version
    - `cd backend && npx tsc --noEmit` exits 0 — confirms TS compilation passes
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | head -20 && echo "---" && npm run test:unit 2>&1 | tail -10</automated>
  </verify>
  <done>
    Prisma 6.x installed, client regenerated, TypeScript compiles, and all unit tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full test suite and verify no regressions</name>
  <files>backend/package.json, backend/package-lock.json</files>
  <read_first>backend/package.json</read_first>
  <action>
    Run the full test suite to verify the Prisma 6.x upgrade introduces no regressions.

    **Steps:**

    1. Run unit tests:
    ```bash
    cd backend
    npm run test:unit 2>&1 | tail -30
    ```

    2. Run integration tests (requires test DB):
    ```bash
    cd backend
    npm run test:integration 2>&1 | tail -30
    ```

    3. If either test suite fails:
    - Read the failure output to determine the cause
    - Check if the issue is related to Prisma 6.x API changes
    - Fix forward per D-06 — address compatibility issues, do NOT revert
    - Document any breaking changes encountered in the SUMMARY.md

    4. Run lint to verify:
    ```bash
    cd backend
    npm run lint 2>&1 | tail -20
    ```

    **Expected outcome:** All tests pass without changes. Research indicates zero breaking changes for this project. However, the approach is fix-forward (D-06).

    **If any test fails** — investigate and fix. Most likely areas:
    - Prisma mock in `__tests__/__mocks__/` — may need updated mock structure for 6.x
    - Integration test DB setup — may need schema resync
  </action>
  <acceptance_criteria>
    - `cd backend && npm run test:unit` exits 0 — all unit tests pass
    - `cd backend && npm run test:integration` exits 0 — all integration tests pass
    - `cd backend && npm run lint` exits 0 — no lint errors
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npm run test:unit 2>&1 | tail -15 && echo "===" && npm run test:integration 2>&1 | tail -15</automated>
  </verify>
  <done>
    Unit tests (241), integration tests, and lint all pass with Prisma 6.x. No regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → backend | Package download from npm during `npm install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-004 | Tampering | npm install | transfer | Package integrity verified by npm registry signatures and lockfile. Project pins to lockfile in CI (`npm ci`). |
| T-02-005 | Spoofing | Prisma 6.x API | mitigate | Full test suite (unit + integration + TypeScript compilation) validates no behavioral changes. Fix-forward approach per D-06. |
</threat_model>

<verification>
1. `cd backend && npx tsc --noEmit` — TypeScript compilation passes
2. `cd backend && npm run test:unit` — All unit tests pass (241)
3. `cd backend && npm run test:integration` — All integration tests pass
4. `cd backend && npm run lint` — No lint errors
5. `grep '"@prisma/client"' backend/package.json | grep '"^6'` — Confirms 6.x version
</verification>

<success_criteria>
1. Prisma and @prisma/client upgraded to latest 6.x
2. Client regenerated without errors via `npx prisma generate`
3. All existing tests pass with zero changes to test code
4. TypeScript compilation succeeds with zero errors
5. Package-lock.json updated with 6.x resolved versions
</success_criteria>

<output>
After completion, create `.planning/phases/02-prisma-modernization/02-02-SUMMARY.md`
</output>
