---
phase: 10-profile-ui-user-topic-route
plan: 03
type: execute
wave: 1
depends_on: [10-01-PLAN.md, 10-02-PLAN.md]
files_modified:
  - frontend/src/components/NavBar.tsx
  - frontend/src/pages/ProfilePage.tsx
autonomous: true
requirements: [NOTIFY-01]
gap_closure: true

must_haves:
  truths:
    - "User's name in navbar is a clickable link that navigates to /profile"
    - "No separate 'Profile' nav link exists in the navbar"
    - "Empty state in Push Notifications section allows user to type a custom topic directly"
    - "Empty state still offers 'Generate random topic' as an alternative in edit mode"
  artifacts:
    - frontend/src/components/NavBar.tsx (username linked to /profile, Profile nav item removed)
    - frontend/src/pages/ProfilePage.tsx (empty state enters edit mode with empty input)
  key_links:
    - "NavBar username Link → /profile route → ProfilePage"
    - "ProfilePage empty state button → topicEdit=true with empty topicValue"
---

<objective>
Fix two UAT gaps: make the navbar username link to Profile (removing the separate Profile nav item), and allow users to type a custom topic from the empty state without being forced to generate a random one first.

Purpose: These are user-reported UX issues that prevent intuitive navigation and topic entry.

Output: Updated NavBar.tsx and ProfilePage.tsx with both gaps closed.
</objective>

<execution_context>
@.config/opencode/gsd-core/workflows/execute-plan.md
@.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-profile-ui-user-topic-route/10-CONTEXT.md
@.planning/phases/10-profile-ui-user-topic-route/10-UAT.md
@frontend/src/components/NavBar.tsx
@frontend/src/pages/ProfilePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make navbar username link to Profile, remove separate Profile nav item</name>
  <files>frontend/src/components/NavBar.tsx</files>
  <read_first>
    - frontend/src/components/NavBar.tsx (current structure — username is span, Profile is separate nav link)
  </read_first>
  <action>
    Two changes in NavBar.tsx:

    1. Remove the `{ to: '/profile', label: 'Profile', parent: false }` entry from the `allLinks` array (currently at line 25). This eliminates the separate "Profile" nav link.

    2. Replace the username `<span>` with a `<Link to="/profile">` that navigates to the Profile page. There are two locations:
       - Desktop: line 53 — `<span className="text-gray-600">{user?.name}</span>` → `<Link to="/profile" className="text-gray-600 hover:text-gray-900 min-h-[44px] inline-flex items-center">{user?.name}</Link>`
       - Mobile: line 89 — same change in the mobile slide-down menu

    Import `Link` is already imported from react-router-dom at line 2. No new imports needed.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Username in navbar is a clickable Link to /profile. No separate "Profile" entry exists in allLinks. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Allow typing custom topic from empty state in Push Notifications</name>
  <files>frontend/src/pages/ProfilePage.tsx</files>
  <read_first>
    - frontend/src/pages/ProfilePage.tsx (empty state block at lines 253-269)
  </read_first>
  <action>
    Update the empty state section in ProfilePage.tsx (lines 253-269) so the user can type a custom topic directly.

    Currently the empty state only shows a "Generate random topic" button that auto-generates a topic and enters edit mode. Change it to:

    1. Show a primary button "Set up notifications" (or "Configure topic") that enters edit mode with an empty `topicValue` so the user can type their own topic.
    2. Keep the "Generate random topic" button as a secondary option — it still generates a random topic and enters edit mode.

    The empty state block should become:
    ```
    <div>
      <p className="text-sm text-gray-500 mb-3">Topic required for notifications</p>
      <div className="flex gap-2">
        <button
          type="button"
          onClick={() => { setTopicValue(''); setTopicEdit(true); setTopicError(null); }}
          className="bg-indigo-600 text-white px-4 py-2 min-h-[44px] rounded-lg hover:bg-indigo-700"
        >
          Set up notifications
        </button>
        <button
          type="button"
          onClick={() => {
            const topic = generateRandomTopic(user?.name ?? 'user')
            setTopicValue(topic)
            setTopicEdit(true)
            setTopicError(null)
          }}
          className="bg-white border border-gray-300 text-gray-700 px-3 py-2 min-h-[44px] rounded-lg text-sm hover:bg-gray-50"
        >
          Generate random topic
        </button>
      </div>
    </div>
    ```

    This gives the user two clear options: type their own (primary button) or generate a random one (secondary button). Both enter edit mode, but the primary button leaves the input empty for custom typing.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Empty state shows "Set up notifications" primary button (enters edit mode with empty input) and "Generate random topic" secondary button. User can type a custom topic without being forced to generate one. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→UI | User interacts with navbar link and empty state buttons |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering | topic input (empty state) | low | accept | Backend validates format via Zod/regex; frontend change is UX-only |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this plan |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
- Manual: Click username in navbar → navigates to Profile page
- Manual: No separate "Profile" link in navbar
- Manual: Empty state in Push Notifications shows "Set up notifications" button and "Generate random topic" button
- Manual: Click "Set up notifications" → edit mode opens with empty input, user can type custom topic
- Manual: Click "Generate random topic" → edit mode opens with generated topic
</verification>

<success_criteria>
- Username in navbar is a clickable Link to /profile
- No "Profile" entry in allLinks array
- Empty state has "Set up notifications" primary button that enters edit mode with empty input
- Empty state has "Generate random topic" secondary button that enters edit mode with generated topic
- TypeScript compiles, no regressions
</success_criteria>

<output>
Create `.planning/phases/10-profile-ui-user-topic-route/10-03-SUMMARY.md` when done
</output>
