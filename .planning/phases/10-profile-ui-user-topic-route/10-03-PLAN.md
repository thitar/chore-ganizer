---
phase: 10-profile-ui-user-topic-route
plan: 03
type: gap_closure
wave: 1
depends_on: [10-01-PLAN.md, 10-02-PLAN.md]
files_modified:
  - frontend/src/components/NavBar.tsx
  - frontend/src/pages/ProfilePage.tsx
autonomous: true
requirements: [NOTIFY-01]
gap_closure: true
gap_ids:
  - "User's name in navbar links to Profile page (no separate 'Profile' nav item)"
  - "User can type a custom topic or generate a random one from the empty state"

must_haves:
  truths:
    - "User's name in navbar is a link to /profile (replaces separate 'Profile' nav item)"
    - "Empty state shows text input for custom topic alongside 'Generate random topic' button"
    - "Custom topic input validates format before save (12-64 chars, [A-Za-z0-9_-])"
    - "Both custom topic and generated topic can be saved successfully"
  artifacts:
    - frontend/src/components/NavBar.tsx (username link to profile, remove Profile nav item)
    - frontend/src/pages/ProfilePage.tsx (empty state with custom topic input)
  key_links:
    - "NavBar username link → /profile route"
    - "ProfilePage empty state → topic input + Generate random topic button"
---

<objective>
Close two major UAT gaps from Phase 10 verification:
1. User's name in navbar should link to Profile page (no separate 'Profile' nav item)
2. User should be able to type a custom topic or generate a random one from the empty state

Purpose: These gaps were identified during human verification and are blocking Phase 10 completion.

Output: Updated NavBar and ProfilePage components that meet all success criteria.
</objective>

<execution_context>
@/home/thitar/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/thitar/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-profile-ui-user-topic-route/10-CONTEXT.md
@.planning/phases/10-profile-ui-user-topic-route/10-UI-SPEC.md
@.planning/phases/10-profile-ui-user-topic-route/10-VERIFICATION.md
@.planning/phases/10-profile-ui-user-topic-route/10-UAT.md
@frontend/src/components/NavBar.tsx
@frontend/src/pages/ProfilePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update NavBar to link username to Profile page</name>
  <files>frontend/src/components/NavBar.tsx</files>
  <read_first>
    - frontend/src/components/NavBar.tsx (current implementation)
    - frontend/src/pages/ProfilePage.tsx (to understand profile route)
  </read_first>
  <action>
    In NavBar.tsx:
    1. Remove the "Profile" link from the `allLinks` array (line 25: `{ to: '/profile', label: 'Profile', parent: false }`)
    2. In the desktop user section (lines 52-61), change the `<span className="text-gray-600">{user?.name}</span>` to a `<Link>` component that navigates to `/profile`:
       - Use `import { Link } from 'react-router-dom'` (already imported)
       - Replace `<span>` with `<Link to="/profile" className="text-gray-600 hover:text-gray-900">{user?.name}</Link>`
    3. In the mobile slide-down menu (lines 88-89), make the same change:
       - Replace `<span className="text-gray-600">{user?.name}</span>` with `<Link to="/profile" onClick={() => setMenuOpen(false)} className="text-gray-600 hover:text-gray-900">{user?.name}</Link>`
    4. Ensure the link has appropriate styling to match the existing design (text-gray-600, hover:text-gray-900)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>NavBar shows username as a link to /profile. Profile nav item removed. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update ProfilePage empty state to allow custom topic input</name>
  <files>frontend/src/pages/ProfilePage.tsx</files>
  <read_first>
    - frontend/src/pages/ProfilePage.tsx (current empty state implementation lines 254-269)
    - .planning/phases/10-profile-ui-user-topic-route/10-UI-SPEC.md (empty state design)
    - .planning/phases/10-profile-ui-user-topic-route/10-CONTEXT.md (decisions D-11)
  </read_first>
  <action>
    In ProfilePage.tsx, update the empty state section (lines 254-269) to allow both custom topic input and random generation:

    Current empty state (lines 254-269):
    ```tsx
    <div>
      <p className="text-sm text-gray-500 mb-3">Topic required for notifications</p>
      <button
        type="button"
        onClick={() => {
          const topic = generateRandomTopic(user?.name ?? 'user')
          setTopicValue(topic)
          setTopicEdit(true)
          setTopicError(null)
        }}
        className="bg-white border border-gray-300 text-gray-700 px-3 py-2 min-h-[44px] rounded-lg text-sm hover:bg-gray-50"
      >
        Generate random topic
      </button>
    </div>
    ```

    Replace with:
    ```tsx
    <div className="space-y-3">
      <p className="text-sm text-gray-500">Topic required for notifications</p>
      <div>
        <label htmlFor="topic-input-empty" className="block text-sm font-normal text-gray-700 mb-1">Topic</label>
        <input
          id="topic-input-empty"
          type="text"
          value={topicValue}
          onChange={(e) => setTopicValue(e.target.value)}
          placeholder="Enter a custom topic (12-64 chars, letters, numbers, hyphens, underscores)"
          className="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-primary-ring font-mono"
        />
      </div>
      <div className="flex gap-2">
        <button
          type="button"
          onClick={() => {
            const topic = generateRandomTopic(user?.name ?? 'user')
            setTopicValue(topic)
            setTopicEdit(true)
            setTopicError(null)
          }}
          className="bg-white border border-gray-300 text-gray-700 px-3 py-2 min-h-[44px] rounded-lg text-sm hover:bg-gray-50"
        >
          Generate random topic
        </button>
      </div>
      {topicError && <div className="bg-red-50 text-red-600 p-3 rounded text-sm">{topicError}</div>}
      <div className="flex gap-2">
        <button
          type="button"
          onClick={handleTopicSave}
          disabled={isUpdatingTopic || !topicValue.trim()}
          className="bg-indigo-600 text-white px-4 py-2 min-h-[44px] rounded-lg hover:bg-indigo-700 disabled:opacity-50"
        >
          {isUpdatingTopic ? 'Saving...' : 'Save'}
        </button>
        <button
          type="button"
          onClick={() => { setTopicValue(''); setTopicError(null); }}
          className="bg-white border border-gray-300 text-gray-700 px-3 py-2 min-h-[44px] rounded-lg text-sm hover:bg-gray-50"
        >
          Clear
        </button>
      </div>
    </div>
    ```

    Also update the `handleTopicSave` function to enter edit mode when saving from empty state:
    - Add `setTopicEdit(true)` before the API call if not already in edit mode
    - Ensure the topic is validated before save (12-64 chars, [A-Za-z0-9_-])

    Add validation in `handleTopicSave`:
    ```tsx
    async function handleTopicSave() {
      setTopicError(null)
      
      // Validate topic format if not empty
      if (topicValue) {
        const regex = /^[-_A-Za-z0-9]{12,64}$/
        if (!regex.test(topicValue)) {
          setTopicError('Topic must be 12-64 characters and contain only letters, numbers, hyphens, and underscores.')
          return
        }
      }
      
      setIsUpdatingTopic(true)
      try {
        await usersApi.updateNtfyTopic(topicValue || null)
        queryClient.invalidateQueries({ queryKey: ['users'] })
        queryClient.invalidateQueries({ queryKey: ['auth', 'me'] })
        setTopicEdit(false)
        setTopicSuccess('Topic saved!')
      } catch (err: any) {
        if (err?.response?.status === 409) {
          setTopicError('This topic is already in use. Please choose another.')
        } else {
          setTopicError(err?.response?.data?.error?.message ?? 'Failed to update topic.')
        }
      } finally {
        setIsUpdatingTopic(false)
      }
    }
    ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Empty state shows text input for custom topic alongside 'Generate random topic' button. Validation prevents invalid topics. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Topic value sent via PUT /me/ntfy-topic (frontend validates format before send) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering | topic input field | low | mitigate | Frontend validates format (12-64 chars, regex) before sending to backend |
| T-10-07 | Information Disclosure | navbar profile link | low | accept | Username link to profile is intentional UX improvement |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
- `cd frontend && npm test` — all existing tests pass (no regressions)
- Manual: NavBar shows username as a link to /profile (no separate Profile nav item)
- Manual: Empty state shows text input for custom topic with placeholder text
- Manual: "Generate random topic" button still works and fills the input
- Manual: Custom topic validation shows error for invalid format
- Manual: Save button disabled when input is empty
- Manual: Clear button resets the input field
</verification>

<success_criteria>
- User's name in navbar is a link to /profile (replaces separate 'Profile' nav item)
- Empty state shows text input for custom topic alongside 'Generate random topic' button
- Custom topic input validates format before save (12-64 chars, [A-Za-z0-9_-])
- Both custom topic and generated topic can be saved successfully
- TypeScript compiles, existing tests pass
</success_criteria>

<output>
Create `.planning/phases/10-profile-ui-user-topic-route/10-03-SUMMARY.md` when done
</output>
