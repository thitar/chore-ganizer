---
phase: 10-profile-ui-user-topic-route
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/__tests__/services/users.service.test.ts
  - backend/src/services/users.service.ts
  - backend/src/routes/users.routes.ts
autonomous: true
requirements: [NOTIFY-01]

must_haves:
  truths:
    - "updateNtfyTopic validates topic format with regex ^[-_A-Za-z0-9]{12,64}$ and throws 400 on invalid input"
    - "updateNtfyTopic checks uniqueness via prisma.user.findFirst excluding current user and throws 409 on conflict"
    - "updateNtfyTopic accepts null or empty string to clear topic, setting ntfyTopic to null"
    - "updateNtfyTopic returns updated user object with ntfyTopic field in select"
    - "PUT /me/ntfy-topic route requires authenticate middleware and delegates to updateNtfyTopic"
  artifacts:
    - backend/src/services/users.service.ts (updateNtfyTopic function added)
    - backend/src/routes/users.routes.ts (PUT /me/ntfy-topic route added)
    - backend/src/__tests__/services/users.service.test.ts (updateNtfyTopic tests added)
  key_links:
    - "users.service.updateNtfyTopic → prisma.user.findFirst (uniqueness check)"
    - "users.service.updateNtfyTopic → prisma.user.update (persist topic)"
    - "users.routes PUT /me/ntfy-topic → users.service.updateNtfyTopic"
---

<objective>
Implement backend updateNtfyTopic service function and PUT /me/ntfy-topic route with TDD.

Purpose: Backend must validate topic format, check uniqueness, and persist the ntfy topic to the User record. This is the API contract that the frontend Profile page will consume in Plan 2.

Output: Working, tested `updateNtfyTopic` service function and `PUT /me/ntfy-topic` route.
</objective>

<execution_context>
@.config/opencode/gsd-core/workflows/execute-plan.md
@.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-profile-ui-user-topic-route/10-CONTEXT.md
@.planning/phases/10-profile-ui-user-topic-route/10-RESEARCH.md
@.planning/phases/09-foundation/09-01-SUMMARY.md
@.planning/phases/09-foundation/09-02-SUMMARY.md
@backend/src/services/users.service.ts
@backend/src/routes/users.routes.ts
@backend/src/__tests__/services/users.service.test.ts
</context>

<feature>
  <name>updateNtfyTopic service + PUT /me/ntfy-topic route</name>
  <files>backend/src/services/users.service.ts, backend/src/routes/users.routes.ts, backend/src/__tests__/services/users.service.test.ts</files>
  <behavior>
    - updateNtfyTopic(1, 'chore-alice-a1b2c3') → returns user with ntfyTopic set
    - updateNtfyTopic(1, null) → returns user with ntfyTopic null (clears topic)
    - updateNtfyTopic(1, '') → returns user with ntfyTopic null (empty string treated as clear)
    - updateNtfyTopic(1, 'short') → throws AppError 400 (too short, < 12 chars)
    - updateNtfyTopic(1, 'has spaces!') → throws AppError 400 (invalid chars)
    - updateNtfyTopic(1, 'chore-alice-a1b2c3') when another user has that topic → throws AppError 409
    - updateNtfyTopic(1, 'chore-alice-a1b2c3') when current user already has it → succeeds (no self-conflict)
    - PUT /me/ntfy-topic with valid topic → 200 with updated user
    - PUT /me/ntfy-topic without auth → 401
  </behavior>
  <implementation>
    Follow existing updateColor pattern in users.service.ts. Add NTFY_TOPIC_REGEX constant. Add updateNtfyTopic function with: null/empty check → normalize to null, format validation via regex, uniqueness check via prisma.user.findFirst excluding current user, prisma.user.update with ntfyTopic in select. Add PUT /me/ntfy-topic route to users.routes.ts following PUT /me/color pattern. Add findFirst to prisma mock in test file.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for updateNtfyTopic</name>
  <files>backend/src/__tests__/services/users.service.test.ts</files>
  <read_first>
    - backend/src/__tests__/services/users.service.test.ts (existing test structure and mock pattern)
    - backend/src/services/users.service.ts (updateColor pattern to mirror)
  </read_first>
  <behavior>
    - Test: updates ntfyTopic with valid topic → prisma.user.update called with ntfyTopic in data and select
    - Test: clears ntfyTopic when null passed → prisma.user.update called with ntfyTopic: null
    - Test: clears ntfyTopic when empty string passed → prisma.user.update called with ntfyTopic: null
    - Test: throws 400 on topic too short (< 12 chars)
    - Test: throws 400 on topic with invalid characters (spaces, special chars)
    - Test: throws 409 when topic already taken by another user
    - Test: succeeds when topic is already owned by same user (no self-conflict)
    - Test: throws 400 on topic too long (> 64 chars)
  </behavior>
  <action>
    Add `findFirst: jest.fn()` to the prisma.user mock in the top-level jest.mock block (line 4). Add a new `describe('usersService.updateNtfyTopic')` block after the existing `updateColor` tests. Write 8 test cases per the behavior spec above. Each test should mock the appropriate prisma methods and assert the correct calls. Run tests to confirm they fail (RED phase) — the function does not exist yet.
  </action>
  <verify>
    <automated>cd backend && npm test -- --testNamePattern="updateNtfyTopic" 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 tests exist and fail with "usersService.updateNtfyTopic is not a function" or similar — RED phase confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement updateNtfyTopic service + route</name>
  <files>backend/src/services/users.service.ts, backend/src/routes/users.routes.ts</files>
  <read_first>
    - backend/src/services/users.service.ts (full file — add updateNtfyTopic after updateColor)
    - backend/src/routes/users.routes.ts (full file — add route after /me/color)
    - backend/src/middleware/errorHandler.ts (AppError class signature)
  </read_first>
  <action>
    In users.service.ts: Add `const NTFY_TOPIC_REGEX = /^[-_A-Za-z0-9]{12,64}$/` after the existing HEX_REGEX. Add `updateNtfyTopic(userId: number, ntfyTopic: string | null)` function that: (1) if ntfyTopic is null or empty string, normalizes to null and calls prisma.user.update with `{ ntfyTopic: null }` and select including ntfyTopic; (2) validates format with NTFY_TOPIC_REGEX, throws AppError 400 with message per D-13 on failure; (3) checks uniqueness via prisma.user.findFirst where `{ ntfyTopic, id: { not: userId } }`, throws AppError 409 'This topic is already in use. Please choose another.' if found; (4) calls prisma.user.update with `{ ntfyTopic }` and select `{ id: true, name: true, email: true, role: true, color: true, ntfyTopic: true }`. Return the updated user object.

    In users.routes.ts: Add `router.put('/me/ntfy-topic', authenticate, async (req, res, next) => { ... })` after the /me/color route. Destructure `{ ntfyTopic }` from req.body, call `usersService.updateNtfyTopic(req.session.userId!, ntfyTopic)`, return `{ success: true, data: user, error: null }`.

    Run tests to confirm they pass (GREEN phase).
  </action>
  <verify>
    <automated>cd backend && npm test -- --testNamePattern="updateNtfyTopic" 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 updateNtfyTopic tests pass. PUT /me/ntfy-topic route exists and delegates to the service. GREEN phase confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | User submits ntfy topic string via PUT /me/ntfy-topic |
| API→DB | Topic written to User.ntfyTopic with @unique constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | ntfy topic input | medium | mitigate | Zod/regex validation rejects special chars; 12-char minimum prevents weak topics |
| T-10-02 | Information Disclosure | 409 error message | low | accept | Error says "topic in use" without revealing which user has it |
| T-10-03 | Spoofing | topic impersonation | low | accept | 12-char minimum makes brute force impractical for family-sized user base |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase |
</threat_model>

<verification>
- `cd backend && npm test` — all tests pass including new updateNtfyTopic tests
- `cd backend && npm run build` — TypeScript compiles without errors
</verification>

<success_criteria>
- updateNtfyTopic validates format (12-64 chars, alphanumeric + hyphens + underscores)
- updateNtfyTopic checks uniqueness and returns 409 on conflict
- updateNtfyTopic clears topic on null/empty input
- PUT /me/ntfy-topic route exists and requires authentication
- All existing + new tests pass
</success_criteria>

<output>
Create `.planning/phases/10-profile-ui-user-topic-route/10-01-SUMMARY.md` when done
</output>
