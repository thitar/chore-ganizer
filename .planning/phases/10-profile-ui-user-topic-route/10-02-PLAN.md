---
phase: 10-profile-ui-user-topic-route
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/users.api.ts
  - frontend/src/pages/ProfilePage.tsx
autonomous: true
requirements: [NOTIFY-01]

must_haves:
  truths:
    - "User opens Profile page and sees Push Notifications section after Display Color with help text per D-10"
    - "User with no topic sees empty state 'Topic required for notifications' with Generate random topic button per D-11"
    - "User with a topic sees their topic displayed with a Change button"
    - "Clicking Change enters edit mode with topic input, Generate random topic button, and Save/Cancel buttons per D-07"
    - "Generate random topic produces format chore-{username}-{6chars} per D-01, D-02"
    - "Save calls PUT /api/users/me/ntfy-topic and shows green toast 'Topic saved!' per D-12"
    - "409 Conflict shows inline error 'This topic is already in use. Please choose another.' per D-13"
    - "Parent users see Family Topics section with cards for each other family member per D-05, D-06"
    - "Family card edit mode works identically to own topic edit with Generate random topic available per D-08"
    - "Clearing topic (empty input) sends null to API and silences notifications per ROADMAP success criterion 5"
  artifacts:
    - frontend/src/api/users.api.ts (updateNtfyTopic function + ntfyTopic on UserSummary)
    - frontend/src/pages/ProfilePage.tsx (Push Notifications section + Family Topics cards)
  key_links:
    - "users.api.updateNtfyTopic → PUT /api/users/me/ntfy-topic"
    - "ProfilePage → useUsers hook → users.api.getAll (family topics data)"
    - "ProfilePage Push Notifications section → users.api.updateNtfyTopic (save mutation)"
---

<objective>
Add Push Notifications section to Profile page with own topic edit, random topic generation, and parent-only Family Topics cards.

Purpose: This is the user-observable deliverable for NOTIFY-01 — users can set, change, or clear their ntfy topic from the Profile page.

Output: Fully functional Profile page with Push Notifications section and Family Topics cards for parents.
</objective>

<execution_context>
@.config/opencode/gsd-core/workflows/execute-plan.md
@.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-profile-ui-user-topic-route/10-CONTEXT.md
@.planning/phases/10-profile-ui-user-topic-route/10-RESEARCH.md
@.planning/phases/10-profile-ui-user-topic-route/10-UI-SPEC.md
@.planning/phases/09-foundation/09-01-SUMMARY.md
@frontend/src/pages/ProfilePage.tsx
@frontend/src/api/users.api.ts
@frontend/src/hooks/useUsers.tsx
@frontend/src/hooks/useAuth.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updateNtfyTopic to users.api.ts and update UserSummary</name>
  <files>frontend/src/api/users.api.ts</files>
  <read_first>
    - frontend/src/api/users.api.ts (existing API functions and interface shapes)
  </read_first>
  <action>
    In users.api.ts: (1) Add `ntfyTopic: string | null` to the `UserSummary` interface (after `color`). (2) Add `ntfyTopic` to the `UserWithEmail` interface (it extends UserSummary, so it inherits automatically). (3) Add a new exported function `updateNtfyTopic(ntfyTopic: string | null): Promise<UserWithEmail>` that calls `api.put('/me/ntfy-topic', { ntfyTopic })` and returns `response.data.data`. This mirrors the existing `updateColor` function pattern. Empty string should be converted to null before the API call: `const payload = ntfyTopic === '' ? null : ntfyTopic`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>users.api.ts has ntfyTopic on UserSummary, updateNtfyTopic function exported, TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Push Notifications section and Family Topics cards to ProfilePage</name>
  <files>frontend/src/pages/ProfilePage.tsx</files>
  <read_first>
    - frontend/src/pages/ProfilePage.tsx (full existing page — sections end at Display Color card)
    - frontend/src/api/users.api.ts (UserSummary now includes ntfyTopic)
    - frontend/src/hooks/useAuth.tsx (user object shape — has role field)
    - frontend/src/hooks/useUsers.tsx (returns users array with UserSummary items)
    - .planning/phases/10-profile-ui-user-topic-route/10-UI-SPEC.md (layout contract, interaction states, copy)
    - .planning/phases/10-profile-ui-user-topic-route/10-CONTEXT.md (decisions D-01 through D-13)
  </read_first>
  <action>
    Add a "Push Notifications" section to ProfilePage.tsx after the Display Color card (per D-09). The section includes:

    **State additions:** Add useState for `topicEdit` (boolean), `topicValue` (string), `topicError` (string|null), `topicSuccess` (string|null), `isUpdatingTopic` (boolean). Add useEffect for topicSuccess auto-dismiss (3s, matches existing pattern).

    **generateRandomTopic function:** Per D-01/D-02, generate `chore-${username.toLowerCase()}-${6 random alphanumeric lowercase chars}`. Use `Math.random()` with chars `abcdefghijklmnopqrstuvwxyz0123456789`. The username comes from `user?.name ?? 'user'`.

    **Own topic section (inside Push Notifications card):**
    - View mode (topic set): Show topic value in monospace font, "Change" button to enter edit mode
    - View mode (no topic / D-11): Show "Topic required for notifications" text, "Generate random topic" button that sets topicValue and enters edit mode
    - Edit mode: Topic input (text, font-mono), "Generate random topic" button, "Save" + "Cancel" buttons. Save calls `usersApi.updateNtfyTopic(topicValue || null)`, invalidates `['users']` and `['auth', 'me']` queries, shows success toast per D-12. Cancel reverts to view mode.

    **Family Topics section (PARENT only, per D-05):**
    - Check `user?.role === 'PARENT'` to conditionally render
    - Filter `users` array to exclude current user: `users.filter(u => u.id !== user?.id)`
    - "Family Topics" heading (h4, matches section title styling)
    - Card per family member (bg-gray-50 rounded-lg p-4 mb-3 per UI-SPEC): Show name, topic or "Not set" (per D-06), Edit button
    - Edit mode per D-07: Inline input + Generate random topic (per D-08) + Save/Cancel
    - Each card has independent edit state (use a `Map<number, boolean>` or object for tracking which card is in edit mode)

    **Styling per UI-SPEC:**
    - Card: `bg-white rounded-lg shadow p-6 mb-6` (matches existing cards)
    - Topic input: `w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-indigo-500 font-mono`
    - Primary button: `bg-indigo-600 text-white px-4 py-2 min-h-[44px] rounded-lg hover:bg-indigo-700 disabled:opacity-50`
    - Secondary button: `bg-white border border-gray-300 text-gray-700 px-3 py-2 min-h-[44px] rounded-lg text-sm hover:bg-gray-50`
    - Family card: `bg-gray-50 rounded-lg p-4 mb-3`
    - Error: `bg-red-50 text-red-600 p-3 rounded text-sm`
    - Success toast: `fixed top-4 right-4 z-50 bg-green-50 text-green-700 px-4 py-2 rounded-lg shadow-md` (matches existing pattern)

    **Toast stacking:** If both passwordSuccess and topicSuccess are active, both toasts appear. Use separate fixed positions or stack them (the existing code already handles this for password/color — follow the same approach).

    **Error handling:** Catch 409 specifically — display per D-13: "This topic is already in use. Please choose another." For other errors, display server message or generic fallback per UI-SPEC.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>ProfilePage renders Push Notifications section after Display Color. Own topic shows view/edit modes with Generate random topic. Parent users see Family Topics cards. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Topic value sent via PUT /me/ntfy-topic (frontend validates format before send) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | topic input field | low | mitigate | Backend validates format; frontend input is text-only, React escapes output |
| T-10-05 | Information Disclosure | family topics display | low | accept | Parents see children's topics — this is intentional per D-05; topics are access tokens but family is trusted |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
- `cd frontend && npm test` — all existing tests pass (no regressions)
- Manual: Profile page shows Push Notifications section after Display Color
- Manual: Empty state shows "Topic required for notifications" with Generate button
- Manual: Edit mode shows input, Generate random, Save/Cancel
- Manual: Parent users see Family Topics cards for other family members
</verification>

<success_criteria>
- Push Notifications section renders after Display Color per D-09
- Empty state shows "Topic required for notifications" per D-11
- Generate random topic produces chore-{username}-{6chars} per D-01/D-02
- Save calls updateNtfyTopic and shows green toast "Topic saved!" per D-12
- 409 Conflict shows "This topic is already in use. Please choose another." per D-13
- Parent users see Family Topics cards per D-05/D-06
- Family card edit mode works with Generate random topic per D-07/D-08
- Clearing topic sends null to API per ROADMAP success criterion 5
- TypeScript compiles, existing tests pass
</success_criteria>

<output>
Create `.planning/phases/10-profile-ui-user-topic-route/10-02-SUMMARY.md` when done
</output>
