---
phase: 03-architecture-and-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/notificationService.ts (DELETE)
  - backend/src/services/emailService.ts (RENAME → email.service.ts)
  - backend/src/services/email.service.ts (RENAMED FROM emailService.ts)
  - backend/src/__tests__/services/emailService.test.ts (IMPORT PATH UPDATE)
autonomous: true
requirements: [TECH-05]
must_haves:
  truths:
    - "notificationService.ts is deleted — confirmed dead code (0 production imports)"
    - "emailService.ts is renamed to email.service.ts — all imports updated"
    - "All existing tests pass after rename"
  artifacts:
    - path: "backend/src/services/email.service.ts"
      provides: "Renamed email dispatch service (was emailService.ts)"
      min_lines: 120
    - path: "backend/src/services/notificationService.ts"
      provides: "DELETED — dead code, 0 production imports"
      exists: false
  key_links:
    - from: "backend/src/__tests__/services/emailService.test.ts"
      to: "../../services/email.service"
      via: "import path update"
      pattern: "email\\.service"
---

<objective>
Standardize service file naming to `dot.case.ts` convention by deleting dead `notificationService.ts` and renaming `emailService.ts` → `email.service.ts`.

**Purpose:** TECH-05 — 13 of 17 services already use `dot.case.ts` naming. This brings the last two holdouts into compliance. `notificationService.ts` (214 lines, 0 production imports) is dead code per ARCH-01, confirmed by grep scan.

**Output:** 
- `backend/src/services/notificationService.ts` deleted
- `backend/src/services/emailService.ts` renamed to `email.service.ts`
- Import path updated in test file
- All tests pass
</objective>

<execution_context>
@/home/thitar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/thitar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md §37-48
@.planning/phases/03-architecture-and-performance/03-CONTEXT.md D-09, D-10

### Verified import status
- `notificationService.ts` → grep confirmed **0 production imports** (dead code)
- `emailService.ts` → imported by `notificationService.ts` (being deleted) + `emailService.test.ts` (test file, will update)
- No barrel files or re-exports reference either file
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead notificationService.ts</name>
  <files>backend/src/services/notificationService.ts</files>
  <action>
    Delete `backend/src/services/notificationService.ts` entirely.

    Pre-deletion verification (performed before deletion):
    - Run `grep -rn "notificationService" backend/src/ --include="*.ts" | grep -v node_modules | grep -v ".test."` to confirm zero production imports
    - The file is confirmed dead code: 214 lines, exports `sendNotification`, `notifyChoreAssigned`, `notifyChoreDueSoon`, `notifyChoreCompleted`, `notifyChoreOverdue`, `notifyPointsEarned` — none used elsewhere

    Deletion:
    - Delete the file using `rm`
    - Do NOT create any replacement — the code is truly dead. `notifications.service.ts` (in-app notification CRUD, 174 lines) is the active service and is not affected.
    - After deletion, run `backend/tsc --noEmit` to confirm zero compilation errors.
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/backend && ls src/services/notificationService.ts 2>&1; echo "---"; npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>
    File `notificationService.ts` no longer exists. TypeScript compilation passes with zero errors. No remaining imports reference it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename emailService.ts → email.service.ts</name>
  <files>
    backend/src/services/emailService.ts
    backend/src/services/email.service.ts
    backend/src/__tests__/services/emailService.test.ts
  </files>
  <action>
    Rename `backend/src/services/emailService.ts` to `backend/src/services/email.service.ts`.

    This is purely a rename — the file content stays identical.

    After rename, update the import in `backend/src/__tests__/services/emailService.test.ts`:
    - Change: `from '../../services/emailService'`
    - To: `from '../../services/email.service'`

    Do NOT rename the test file itself — test files use `PascalCase` as per project convention (see STRUCTURE.md §Naming Conventions).

    After updating:
    1. Run `npx tsc --noEmit` to verify compilation
    2. Run `npm test -- --testPathPattern=emailService` to verify tests still pass
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/backend && ls src/services/email.service.ts 2>/dev/null && npx tsc --noEmit --pretty 2>&1 | tail -5 && npm test -- --testPathPattern=emailService 2>&1 | tail -15</automated>
  </verify>
  <done>
    `email.service.ts` exists. `emailService.ts` no longer exists. TypeScript compilation passes. Email service tests (sendEmail, sendChoreAssigned, sendChoreCompleted, sendPointsEarned) all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure refactoring — no new trust boundaries, no external interfaces changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | notificationService.ts deletion | mitigate | Verify zero production imports via grep before deletion (Task 1) |
| T-03-02 | Spoofing | email.service.ts rename | mitigate | Test file import updated, TypeScript compilation confirms no stale references |
</threat_model>

<verification>
1. ✅ `notificationService.ts` deleted — confirm file absent, zero compilation errors
2. ✅ `email.service.ts` present — confirm file exists, `emailService.ts` absent  
3. ✅ Test file import updated — confirm `emailService.test.ts` imports from `email.service`
4. ✅ `npm test -- --testPathPattern=emailService` passes all 8 email service tests
5. ✅ `npm run test:unit` passes (full unit test suite)
</verification>

<success_criteria>
- TECH-05 complete: all service files use `dot.case.ts` convention
- Dead code eliminated: `notificationService.ts` deleted
- Email service still functional: renamed, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-architecture-and-performance/03-01-SUMMARY.md`
</output>
