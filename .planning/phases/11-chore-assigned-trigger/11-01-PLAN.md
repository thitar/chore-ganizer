---
phase: 11-chore-assigned trigger
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/__tests__/services/assignment.service.test.ts
  - backend/src/services/assignment.service.ts
autonomous: true
requirements:
  - NOTIFY-02

must_haves:
  truths:
    - "When a parent assigns a chore to a child with an ntfy topic, a fetch POST is sent to {NTFY_BASE_URL}/{topic} with body containing the chore title, priority 3, tags clipboard+bell, and click path /chores/{id}"
    - "When the assigned child has ntfyTopic null, no fetch is called and the assignment still returns 201"
    - "When NTFY_BASE_URL is not configured (isNtfyConfigured false), no fetch is called and the assignment still returns 201"
    - "When fetch throws (ntfy server unreachable), the assignment still succeeds and the error is caught inside sendNtfy"
  artifacts:
    - backend/src/services/assignment.service.ts — create() now imports notifyChoreAssigned, does findUnique with includes, calls void notifyChoreAssigned(enriched), returns enriched
    - backend/src/__tests__/services/assignment.service.test.ts — existing create test updated for enriched return; 4 new notification test cases added
  key_links:
    - assignment.service.create -> notifyChoreAssigned (fire-and-forget, per D-02)
    - notifyChoreAssigned -> sendNtfy (existing from Phase 9)
    - sendNtfy -> global.fetch (ntfy HTTP POST)
---

<objective>
Wire the chore-assigned notification trigger into assignment.service.create() so that when a parent assigns a chore, the recipient automatically receives an ntfy push notification.

Purpose: This completes NOTIFY-02 — the first user-facing notification behavior in v3.1. Without this, assignments are created silently with no push.
Output: Modified assignment.service.ts (notification wiring) + modified assignment.service.test.ts (4 new tests + updated existing test)
</objective>

<execution_context>
@/home/thitar/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/thitar/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-chore-assigned-trigger/11-CONTEXT.md
@.planning/phases/11-chore-assigned-trigger/11-RESEARCH.md
@.planning/phases/11-chore-assigned-trigger/11-PATTERNS.md
@backend/src/services/assignment.service.ts
@backend/src/services/notification.service.ts
@backend/src/services/notification.formatters.ts
@backend/src/__tests__/services/assignment.service.test.ts
@backend/src/__tests__/services/notification.service.test.ts
@backend/src/config/notifications.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write notification wiring tests for assignment.service.create</name>
  <files>backend/src/__tests__/services/assignment.service.test.ts</files>
  <read_first>
    - backend/src/__tests__/services/assignment.service.test.ts (full file — understand mock setup, existing create test at lines 41-63, jest.mock pattern at lines 1-26)
    - backend/src/__tests__/services/notification.service.test.ts (lines 16-22 for jest.spyOn(global, 'fetch') pattern; lines 86-98 for jest.resetModules pattern)
    - backend/src/services/assignment.service.ts (lines 5-23 — current create function that will be modified in Task 2)
    - backend/src/services/notification.service.ts (lines 37-49 — AssignmentWithIncludes type and notifyChoreAssigned wrapper)
    - backend/src/config/notifications.ts (isNtfyConfigured export — needed for ntfy-disabled mock)
  </read_first>
  <behavior>
    - Existing create test ("creates assignment with status PENDING"): After create() is called with valid data, the result should be the enriched object (with template and assignedTo includes), not the bare create result. The test must mock choreAssignment.findUnique to return the enriched shape.
    - Test "fires ntfy fetch with correct payload when child has topic": create() with a child who has ntfyTopic set -> global.fetch is called once -> URL is 'https://ntfy.example.com/alice-topic' -> body is 'Wash Dishes — due 2026-07-15' -> headers include Title='Chore-Ganizer', Priority='3', Tags='clipboard,bell', Click='/chores/1'
    - Test "does not fire ntfy fetch when assignedTo.ntfyTopic is null": create() with enriched assignedTo.ntfyTopic=null -> global.fetch is NOT called -> assignment still succeeds
    - Test "does not fire ntfy fetch when NTFY_BASE_URL is unset": re-require assignment.service with isNtfyConfigured=false -> create() -> global.fetch is NOT called -> assignment still succeeds
    - Test "assignment succeeds even when ntfy fetch throws": global.fetch mock rejects with Error -> create() does NOT throw -> result is the enriched assignment -> global.fetch WAS called
  </behavior>
  <action>
Update the existing create test (lines 42-55) to account for the enriched return:

1. Add a `choreAssignment.findUnique` mock returning an enriched object (with template: { id, title, points, category } and assignedTo: { id, name, color, ntfyTopic }) after the create mock.

2. Change the assertion from `expect(result).toBe(created)` to `expect(result).toBe(enriched)` — the function will return the findUnique result per D-01.

3. Add assertion that findUnique was called with the correct includes (verifying ntfyTopic: true is in the assignedTo select).

Inside the `describe('assignmentService.create', ...)` block, after the existing two tests, add 4 new test cases:

4. **Positive test — fetch fires with correct payload**: Mock global.fetch with jest.spyOn(global, 'fetch').mockResolvedValue(new Response()). Set up create to return bare object, findUnique to return enriched with ntfyTopic='alice-topic'. Call create(). Flush microtasks with `await new Promise(process.nextTick)`. Assert fetch was called with URL 'https://ntfy.example.com/alice-topic', method POST, body containing chore title, headers with Title, Priority='3', Tags='clipboard,bell', Click='/chores/{id}'. Call jest.restoreAllMocks() at end.

5. **Negative test — null topic**: Same setup but enriched object has assignedTo.ntfyTopic=null. Assert global.fetch was NOT called.

6. **Negative test — ntfy disabled**: Use jest.resetModules(), then jest.mock('../../config/notifications', ...) with isNtfyConfigured: false, then jest.mock('../../config/prisma', ...) with the full prisma mock shape. Re-require assignment.service via require(). Set up mocks on the new prisma instance. Call create(). Assert global.fetch was NOT called.

7. **Graceful test — fetch throws**: Mock global.fetch with jest.spyOn(global, 'fetch').mockRejectedValue(new Error('Network error')). Set up create + findUnique. Call create() — it must NOT throw. Assert result equals enriched. Assert global.fetch WAS called.

Pattern references per D-03 through D-06:
- D-03: Full payload verification (URL, body, headers) in the positive test
- D-04: Null-topic path test — no fetch called
- D-05: NTFY disabled path — jest.resetModules + re-mock + re-require
- D-06: Server-unreachable path — fetch throws, assignment succeeds
  </action>
  <verify>
    <automated>cd backend && npx jest --testPathPattern=assignment.service --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The 4 new test cases exist in assignment.service.test.ts inside the `describe('assignmentService.create', ...)` block
    - The existing create test asserts `expect(result).toBe(enriched)` (not `.toBe(created)`)
    - The existing create test asserts that `choreAssignment.findUnique` was called with includes containing `ntfyTopic: true`
    - `npm test -- --testPathPattern=assignment.service` shows all new tests FAILING (RED) because create() does not yet call notifyChoreAssigned
    - Each notification test manages its own jest.spyOn/global.fetch mock and calls jest.restoreAllMocks()
    - The ntfy-disabled test uses jest.resetModules() + re-require pattern
  </acceptance_criteria>
  <done>All new tests written and failing (RED phase). Existing test updated for enriched return. No production code changed yet.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — Wire notifyChoreAssigned into assignment.service.create</name>
  <files>backend/src/services/assignment.service.ts</files>
  <read_first>
    - backend/src/services/assignment.service.ts (full file — current create function at lines 5-23, existing includes pattern at lines 52-54 and 150-155)
    - backend/src/services/notification.service.ts (lines 37-49 — AssignmentWithIncludes type, notifyChoreAssigned signature)
    - backend/src/__tests__/services/assignment.service.test.ts (the tests written in Task 1 — understand expected behavior)
    - backend/src/config/notifications.ts (isNtfyConfigured — already imported by notification.service, no import needed here)
  </read_first>
  <behavior>
    - After calling create() with valid data and a child who has ntfyTopic, the function returns the enriched assignment (with template and assignedTo includes) AND fires a fetch POST to the ntfy server
    - After calling create() with a child whose ntfyTopic is null, the function returns the enriched assignment and does NOT fire fetch
    - After calling create() when isNtfyConfigured is false, the function returns the enriched assignment and does NOT fire fetch
    - After calling create() when fetch throws, the function still returns the enriched assignment (error caught inside sendNtfy)
  </behavior>
  <action>
Modify `backend/src/services/assignment.service.ts`:

1. Add import at line 3 (after existing imports):
   `import { notifyChoreAssigned } from './notification.service'`

2. Replace the `return prisma.choreAssignment.create(...)` block (lines 14-22) with:
   - Store the create result in `const created = await prisma.choreAssignment.create({...})`
   - Add a `findUnique` call with includes matching the pattern from `getAll` (lines 52-54) and `complete` (lines 150-155), but adding `ntfyTopic: true` to the assignedTo select:
     ```
     include: {
       template: { select: { id: true, title: true, points: true, category: true } },
       assignedTo: { select: { id: true, name: true, color: true, ntfyTopic: true } },
     }
     ```
   - Guard and fire: `if (enriched) void notifyChoreAssigned(enriched)` — per D-02, fire-and-forget, never await
   - Return: `return enriched ?? created` — defensive fallback if findUnique somehow returns null

Per D-01: The findUnique is a SEPARATE query after create, not an expansion of create's includes.
Per D-02: The notification fires in the last lines of create(), before the return statement.
</action>
  <verify>
    <automated>cd backend && npx jest --testPathPattern=assignment.service --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - assignment.service.ts imports notifyChoreAssigned from './notification.service'
    - create() stores the prisma.choreAssignment.create result in a variable named `created`
    - create() calls prisma.choreAssignment.findUnique with include containing template (id, title, points, category) and assignedTo (id, name, color, ntfyTopic: true)
    - create() calls `if (enriched) void notifyChoreAssigned(enriched)` — NOT awaited, NOT wrapped in try/catch
    - create() returns `enriched ?? created`
    - `npm test -- --testPathPattern=assignment.service` — ALL tests pass (GREEN phase)
    - `npm test` — full suite passes with no regressions
  </acceptance_criteria>
  <done>create() imports notifyChoreAssigned, does findUnique with includes (including ntfyTopic), fires void notifyChoreAssigned(enriched), returns enriched assignment. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assignment.service.create -> notifyChoreAssigned | Fire-and-forget call — notification failure must not affect assignment creation |
| sendNtfy -> ntfy HTTP server | External HTTP call — server may be unreachable; AbortSignal.timeout(3000) + internal catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Denial of Service | notifyChoreAssigned call in create() | medium | mitigate | Fire-and-forget (void prefix) + sendNtfy internal catch ensures notification failure never blocks or fails the assignment response |
| T-11-02 | Information Disclosure | ntfy topic in fetch URL | low | accept | Topic is already stored in DB (User.ntfyTopic); fetch URL logs only on failure with `[ntfy] send failed` prefix per Phase 9 convention |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this phase — zero npm dependencies added |
</threat_model>

<verification>
- `npm test -- --testPathPattern=assignment.service` — all tests pass
- `npm run lint` — no lint errors in modified files
- `npm test` — full backend test suite passes (no regressions)
</verification>

<success_criteria>
1. assignment.service.create() calls notifyChoreAssigned with the enriched assignment after creating it
2. 4 new test cases pass: positive (fetch fires with correct payload), null-topic (no fetch), ntfy-disabled (no fetch), server-unreachable (assignment succeeds)
3. Existing create test passes with updated assertion for enriched return
4. Full test suite green
</success_criteria>

<output>
Create `.planning/phases/11-chore-assigned-trigger/11-01-SUMMARY.md` when done
</output>
