---
phase: 06-user-management-profile
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - frontend-v2/src/api/users.api.ts
  - frontend-v2/src/hooks/useUsers.tsx
  - frontend-v2/src/pages/UsersPage.tsx
  - frontend-v2/src/components/NavBar.tsx
  - frontend-v2/src/App.tsx
  - frontend-v2/src/__tests__/UsersPage.test.tsx
autonomous: true
requirements: [AUTH-03, AUTH-04]
---

<objective>
UsersPage: API client (extended), hook, parent-only page with create form + delete. NavBar Users link. App route.
</objective>

<context>
@.planning/phases/06-user-management-profile/06-CONTEXT.md
@.planning/phases/06-user-management-profile/06-UI-SPEC.md
@frontend-v2/src/api/templates.api.ts
@frontend-v2/src/hooks/useTemplates.tsx
@frontend-v2/src/pages/TemplatesPage.tsx
@frontend-v2/src/api/users.api.ts
@frontend-v2/src/hooks/useUsers.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API + hook + page + NavBar + route</name>
  <files>frontend-v2/src/api/users.api.ts, frontend-v2/src/hooks/useUsers.tsx, frontend-v2/src/pages/UsersPage.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/App.tsx, frontend-v2/src/__tests__/UsersPage.test.tsx</files>
  <read_first>frontend-v2/src/api/templates.api.ts, frontend-v2/src/hooks/useTemplates.tsx, frontend-v2/src/pages/TemplatesPage.tsx</read_first>

  <action>
    1. Update `frontend-v2/src/api/users.api.ts`:
       - Add `createUser(data)`, `deleteUser(id)`, `updatePassword(...)`, `updateColor(color)` functions
       - Update `UserSummary` interface to include `email`

    2. Update `frontend-v2/src/hooks/useUsers.tsx`:
       - Add `createUserMutation`, `deleteUserMutation`
       - Both invalidate `['users']` on success

    3. Create `frontend-v2/src/pages/UsersPage.tsx` per UI-SPEC:
       - List with 5 columns (Name, Email, Role, Color, Actions)
       - Create form (toggle): name, email, password, role select, color picker
       - Delete confirmation via ConfirmDelete
       - Self-row shows "You" instead of delete button
       - States: loading, error, empty

    4. Update `NavBar.tsx`: Add "Users" link for PARENT only

    5. Update `App.tsx`: Add `/users` route with `ProtectedRoute requiredRole="PARENT"`

    6. Create `UsersPage.test.tsx` with 6+ tests
  </action>

  <verify>
    <automated>cd frontend-v2 && npx vitest run src/__tests__/UsersPage.test.tsx && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - users.api.ts exports new functions
    - useUsers returns new mutations
    - UsersPage shows list + create form + delete
    - NavBar has "Users" link for PARENT only
    - App.tsx has /users route
    - All tests pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd frontend-v2 && npx vitest run` — all pass
- `cd frontend-v2 && npx tsc --noEmit` — no errors
</verification>

<success_criteria>
- API + hook + page + NavBar + route in place
- Create form works
- Delete with confirmation
- Self-row protected
- All tests pass
</success_criteria>
