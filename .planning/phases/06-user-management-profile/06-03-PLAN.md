---
phase: 06-user-management-profile
plan: 03
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - frontend-v2/src/pages/ProfilePage.tsx
  - frontend-v2/src/components/NavBar.tsx
  - frontend-v2/src/App.tsx
  - frontend-v2/src/__tests__/ProfilePage.test.tsx
autonomous: true
requirements: [AUTH-05, AUTH-06]
---

<objective>
ProfilePage: self-service password change + display color update. NavBar Profile link. App route.
</objective>

<context>
@.planning/phases/06-user-management-profile/06-CONTEXT.md
@.planning/phases/06-user-management-profile/06-UI-SPEC.md
@frontend-v2/src/api/users.api.ts
@frontend-v2/src/hooks/useAuth.tsx
@frontend-v2/src/pages/UsersPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile page + NavBar + route</name>
  <files>frontend-v2/src/pages/ProfilePage.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/App.tsx, frontend-v2/src/__tests__/ProfilePage.test.tsx</files>
  <read_first>frontend-v2/src/hooks/useAuth.tsx, frontend-v2/src/pages/UsersPage.tsx</read_first>

  <action>
    1. Create `frontend-v2/src/pages/ProfilePage.tsx` per UI-SPEC:
       - User info card (color swatch, name, email, role)
       - Password change card (current, new, confirm)
       - Color change card (input type="color")
       - Success toasts on each update

    2. Update `NavBar.tsx`: Add "Profile" link for all roles (between My Chores and Points)

    3. Update `App.tsx`: Add `/profile` route with `ProtectedRoute`

    4. Create `ProfilePage.test.tsx` with 6+ tests
  </action>

  <verify>
    <automated>cd frontend-v2 && npx vitest run src/__tests__/ProfilePage.test.tsx && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - ProfilePage shows user info
    - Password change form with 3 fields
    - Color change form
    - Success messages
    - NavBar has Profile link
    - App.tsx has /profile route
    - All tests pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd frontend-v2 && npx vitest run` — all pass
- `cd frontend-v2 && npx tsc --noEmit` — no errors
</verification>

<success_criteria>
- ProfilePage + NavBar + route in place
- Password change works (current password verified)
- Color change works
- All tests pass
</success_criteria>
