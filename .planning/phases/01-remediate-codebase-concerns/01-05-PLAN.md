---
phase: 01-remediate-codebase-concerns
plan: 05
type: tdd
wave: 2
depends_on: []
files_modified:
  - backend/src/services/overdue-penalty.service.ts
  - backend/src/services/overdue-penalty.service.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Overdue chores are penalized exactly once (penaltyApplied flag prevents double-penalty)"
    - "Penalty calculation respects timezone boundaries for due dates"
    - "Leap year February 29 dates are handled correctly"
    - "All edge cases are covered by automated tests"
  artifacts:
    - path: "backend/src/services/overdue-penalty.service.ts"
      provides: "Overdue penalty logic with edge case handling"
      contains: "penaltyApplied"
    - path: "backend/src/services/overdue-penalty.service.test.ts"
      provides: "Overdue penalty edge case tests"
      contains: "penaltyApplied\|leap\|timezone"
  key_links:
    - from: "backend/src/services/overdue-penalty.service.test.ts"
      to: "backend/src/services/overdue-penalty.service.ts"
      via: "import and mock"
      pattern: "import.*overdue-penalty"
---

<objective>
Add comprehensive automated test coverage for overdue penalty edge cases: double-penalty prevention, timezone boundary handling, leap year dates, and DST transitions. Use TDD to specify exact behavior before implementation verification.

Purpose: Prevent financial/currency errors from incorrect penalty calculations and ensure penalty is applied exactly once per overdue chore.
Output: Passing test suite in overdue-penalty.service.test.ts covering all edge cases.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

<interfaces>
From backend/src/services/overdue-penalty.service.ts (relevant functions):
```typescript
export const findOverdueChoresWithoutPenalty = async () => {
  const startOfToday = new Date()
  startOfToday.setUTCHours(0, 0, 0, 0)

  return prisma.choreAssignment.findMany({
    where: {
      dueDate: { lt: startOfToday },
      status: 'PENDING',
      penaltyApplied: false,
    },
    include: { choreTemplate: true, assignedTo: true, assignedBy: true },
  })
}

export const applyOverduePenalty = async (
  assignmentId: number,
  multiplier: number
): Promise<{ penaltyPoints: number; userId: number; choreTitle: string }> => {
  // Calculates penalty as -Math.abs(assignment.choreTemplate.points * multiplier)
  // Updates user.points and sets penaltyApplied = true
}
```
</interfaces>
</context>

<feature>
  <name>Overdue Penalty Edge Case Handling</name>
  <files>backend/src/services/overdue-penalty.service.ts, backend/src/services/overdue-penalty.service.test.ts</files>
  <behavior>
    - Double-penalty prevention: After penaltyApplied is set to true, subsequent calls to findOverdueChoresWithoutPenalty must NOT return the same assignment
    - Timezone boundary: A chore due at 23:59 UTC on day N should NOT be considered overdue at 00:00 UTC on day N+1 (or should it? The test should define the exact boundary)
    - Leap year: February 29 due dates work correctly in non-leap years (treated as March 1 or rejected?)
    - DST transition: Due dates during DST transitions calculate correctly
    - Penalty calculation: Negative integer points, no floating point math
  </behavior>
  <implementation>
    Use Jest with mocked Prisma client. Mock dates using jest.useFakeTimers() or a date-fns mock. Test the service functions in isolation.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>RED: Write failing tests for overdue penalty edge cases</name>
  <files>backend/src/services/overdue-penalty.service.test.ts</files>
  <read_first>
    - backend/src/services/overdue-penalty.service.ts (full file)
    - backend/src/__tests__/test-helpers.ts (mock factories)
    - backend/src/__tests__/__mocks__/database.ts (Prisma mock setup)
    - Any existing service test files for patterns (e.g., backend/src/__tests__/services/*.test.ts)
  </read_first>
  <behavior>
    - Test 1: "should not penalize the same assignment twice" — Mock assignment with penaltyApplied=true. Verify findOverdueChoresWithoutPenalty excludes it. Verify applyOverduePenalty throws if called on already-penalized assignment.
    - Test 2: "should handle timezone boundary at start of day" — Mock dueDate at 23:59 UTC, current time at 00:01 UTC next day. Verify assignment is found as overdue. Mock dueDate at 00:01 UTC, current time at 23:59 UTC same day. Verify NOT found as overdue.
    - Test 3: "should calculate leap year dates correctly" — Mock dueDate as Feb 29 in a leap year. Verify proper handling. Mock dueDate as Feb 29 in non-leap year (if schema allows it — test should document behavior).
    - Test 4: "should use integer math for penalty calculation" — Mock points=3, multiplier=1.5. Verify penaltyPoints is integer (either -4 or -3 depending on rounding strategy).
    - Test 5: "should handle DST transition dates" — Mock dueDate during spring forward / fall back. Verify startOfToday calculation remains consistent.
  </behavior>
  <action>
    Create backend/src/services/overdue-penalty.service.test.ts with edge case tests.

    Setup approach:
    1. Mock Prisma using the project's mock pattern:
       ```typescript
       import { prismaMock } from '../../__tests__/__mocks__/database'
       // OR jest.mock('../../config/database', ...)
       ```
    2. Mock date-fns or use jest.useFakeTimers() to control the current date
    3. Create mock chore assignment objects with required relations (choreTemplate, assignedTo)
    4. Test each function in isolation:
       - `findOverdueChoresWithoutPenalty`
       - `applyOverduePenalty`

    The tests should fail if the current implementation has gaps in these edge cases.
    If tests pass immediately, they serve as regression protection.
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/backend && npm test -- overdue-penalty.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - overdue-penalty.service.test.ts exists with at least 5 edge case tests
    - Tests run and guide implementation (fail if behavior is wrong)
    - Committed: `test(01-05): add overdue penalty edge case tests`
  </done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: Fix overdue penalty implementation for edge cases</name>
  <files>backend/src/services/overdue-penalty.service.ts</files>
  <read_first>
    - backend/src/services/overdue-penalty.service.ts (current state)
    - backend/src/services/overdue-penalty.service.test.ts (test results)
  </read_first>
  <action>
    Run the tests. Fix any failures with minimal changes to overdue-penalty.service.ts.

    Common fixes needed:
    1. If double-penalty test fails: Add guard in applyOverduePenalty to check penaltyApplied before applying
    2. If timezone test fails: Review startOfToday calculation — consider using date-fns startOfDay with timezone awareness
    3. If integer math test fails: Ensure penalty uses Math.round or Math.floor consistently
    4. If DST test fails: Use UTC-based date comparisons to avoid DST ambiguity

    Do NOT refactor the entire service. Only fix the specific edge cases that tests reveal.
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/backend && npm test -- overdue-penalty.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All overdue-penalty.service.test.ts tests pass
    - Implementation changes are minimal and targeted
    - Committed: `feat(01-05): fix overdue penalty edge cases`
  </done>
</task>

<task type="auto" tdd="true">
  <name>REFACTOR: Clean up penalty service if needed</name>
  <files>backend/src/services/overdue-penalty.service.ts</files>
  <read_first>
    - backend/src/services/overdue-penalty.service.ts (current state after fixes)
  </read_first>
  <action>
    Review the service for obvious refactoring opportunities:
    - Extract date boundary calculation to a helper function
    - Improve variable names if unclear
    - Add JSDoc comments for edge case behavior

    Only refactor if improvements are obvious and low-risk. Skip if the code is already clean.
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/backend && npm test -- overdue-penalty.service.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Tests pass after any refactoring
    - Code is well-documented and maintainable
    - If changes made: committed with `refactor(01-05): clean up overdue penalty service`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler → penalty service | Automated job calls penalty service; incorrect logic affects user points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Repudiation | overdue-penalty.service.ts | mitigate | penaltyApplied flag provides audit trail; tests verify single-application |
| T-05-02 | Elevation of Privilege | penalty calculation | mitigate | Integer math prevents floating-point manipulation |
</threat_model>

<verification>
- All overdue-penalty.service.test.ts tests pass
- `npm run test:unit` in backend passes without regressions
- `npm run build` in backend compiles
- `npm run lint` in backend passes
</verification>

<success_criteria>
- Double-penalty prevention verified by test
- Timezone boundary behavior defined and tested
- Leap year handling verified
- Integer penalty calculation confirmed
- 2-3 atomic commits documenting TDD cycle
</success_criteria>

<output>
After completion, create `.planning/phases/01-remediate-codebase-concerns/01-05-SUMMARY.md`
</output>
