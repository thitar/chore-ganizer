---
phase: 01-remediate-codebase-concerns
plan: 04
type: tdd
wave: 2
depends_on:
  - 01-02
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/hooks/useAuth.tsx
  - frontend/src/api/client.test.ts
  - frontend/src/hooks/useAuth.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "API client interceptors handle 401 responses by dispatching auth:unauthorized event"
    - "Network failures produce proper error objects with NETWORK_ERROR code"
    - "useAuth hook listens for auth:unauthorized and clears auth state"
    - "All error handling paths have automated test coverage"
  artifacts:
    - path: "frontend/src/api/client.test.ts"
      provides: "Client error handling tests"
      contains: "401\|NETWORK_ERROR\|500"
    - path: "frontend/src/hooks/useAuth.test.tsx"
      provides: "Auth hook event listener tests"
      contains: "auth:unauthorized"
  key_links:
    - from: "frontend/src/api/client.test.ts"
      to: "frontend/src/api/client.ts"
      via: "import and mock"
      pattern: "import.*client"
    - from: "frontend/src/hooks/useAuth.test.tsx"
      to: "frontend/src/hooks/useAuth.tsx"
      via: "import and render"
      pattern: "import.*useAuth"
---

<objective>
Add automated test coverage for frontend error handling paths: API client interceptors (401 auto-logout, network failures, 500 errors) and the useAuth hook's auth:unauthorized event listener. Use TDD to ensure behavior is specified before implementation verification.

Purpose: Close test coverage gaps in critical error handling paths that affect user session management.
Output: Passing test suites in client.test.ts and useAuth.test.tsx covering all specified error scenarios.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

<interfaces>
From frontend/src/api/client.ts (relevant error handling section):
```typescript
// Response interceptor:
if (status === 401) {
  console.warn('[ApiClient] 🚨 401 UNAUTHORIZED - Session may be invalid:', errorCode, errorMessage)
  window.dispatchEvent(new CustomEvent('auth:unauthorized', {
    detail: { code: errorCode, message: errorMessage }
  }))
}

if (error.response) {
  throw error.response.data
} else if (error.request) {
  throw {
    success: false,
    error: {
      message: 'No response from server',
      code: 'NETWORK_ERROR',
    },
  }
} else {
  throw {
    success: false,
    error: {
      message: error.message || 'Request failed',
      code: 'REQUEST_ERROR',
    },
  }
}
```

From frontend/src/hooks/useAuth.tsx (event listener pattern):
```typescript
// AuthContext listens for 'auth:unauthorized' custom DOM event
// and triggers logout/clear state
```
</interfaces>
</context>

<feature>
  <name>Frontend Error Handling Test Coverage</name>
  <files>frontend/src/api/client.test.ts, frontend/src/hooks/useAuth.test.tsx</files>
  <behavior>
    - 401 response → dispatches CustomEvent('auth:unauthorized') with error detail
    - Network failure (no response) → throws { success: false, error: { message: 'No response from server', code: 'NETWORK_ERROR' } }
    - 500 response → throws error.response.data
    - useAuth hook receives 'auth:unauthorized' event → clears auth state and triggers navigation/re-render
    - Multiple 401 responses → each dispatches event (no deduplication required)
  </behavior>
  <implementation>
    Write tests using Vitest + React Testing Library. Mock axios at the module level. For useAuth tests, render the AuthProvider and simulate the custom event on window.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>RED: Write failing tests for client error handling</name>
  <files>frontend/src/api/client.test.ts</files>
  <read_first>
    - frontend/src/api/client.ts (full file)
    - frontend/src/test/utils.tsx (test utilities)
    - Any existing frontend test files for mocking patterns (e.g., frontend/src/hooks/useAuth.test.tsx)
  </read_first>
  <behavior>
    - Test 1: "should dispatch auth:unauthorized event on 401 response" — Mock 401 with error code UNAUTHORIZED. Verify window.dispatchEvent called with CustomEvent('auth:unauthorized').
    - Test 2: "should throw NETWORK_ERROR on network failure" — Mock axios to reject with request but no response. Verify thrown error has code NETWORK_ERROR.
    - Test 3: "should throw server error data on 500 response" — Mock 500 with specific error payload. Verify thrown error matches the response data.
    - Test 4: "should dispatch auth:unauthorized for each 401 request" — Make two requests that both 401. Verify two events dispatched.
  </behavior>
  <action>
    Create or extend frontend/src/api/client.test.ts with error handling tests.

    Testing approach:
    1. Mock axios using `vi.mock('axios', () => ({ ... }))` or mock the internal axios instance
    2. Spy on `window.dispatchEvent` to verify event dispatching
    3. Use `expect(promise).rejects.toEqual(...)` for error assertions
    4. For network failure, mock axios to reject with `{ request: {} }` but no `response`

    If client.test.ts already exists (from Plan 01-02), append these tests to the same file.
    The tests should exercise the public API methods (get/post/etc.) of ApiClient.
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/frontend && npm test -- client.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - client.test.ts contains tests for 401 event dispatch, network failure, and 500 errors
    - Tests run and either pass (if behavior already correct) or guide verification
    - Tests committed: `test(01-04): add tests for frontend API error handling`
  </done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: Verify client error handling implementation</name>
  <files>frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/api/client.ts (current state)
    - frontend/src/api/client.test.ts (test results from RED phase)
  </read_first>
  <action>
    Run the tests. If any fail, fix the client.ts implementation to match expected behavior.

    Likely issues to fix:
    1. If 401 event dispatch test fails: verify the event is dispatched with correct detail structure
    2. If network error test fails: verify the error object shape matches expectations
    3. If 500 error test fails: verify error.response.data is thrown correctly

    Do NOT change behavior that is not tested. Only make minimal fixes to pass the tests.
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/frontend && npm test -- client.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All client.test.ts tests pass
    - Implementation is minimal (only changed if tests required it)
    - Committed: `feat(01-04): verify frontend API error handling`
  </done>
</task>

<task type="auto" tdd="true">
  <name>RED: Write failing tests for useAuth hook event listener</name>
  <files>frontend/src/hooks/useAuth.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useAuth.tsx (full implementation)
    - frontend/src/hooks/useAuth.test.tsx (if exists)
    - frontend/src/test/utils.tsx (renderWithRouter and other helpers)
  </read_first>
  <behavior>
    - Test 1: "should clear auth state when auth:unauthorized event is dispatched" — Render AuthProvider with authenticated state, dispatch event, verify user becomes null/unauthenticated.
    - Test 2: "should redirect to login on auth:unauthorized" — If useAuth triggers navigation on logout, verify redirect occurs.
    - Test 3: "should remove event listener on unmount" — Verify no memory leaks from lingering event listeners.
  </behavior>
  <action>
    Create or extend frontend/src/hooks/useAuth.test.tsx with event listener tests.

    Testing approach:
    1. Use `renderWithRouter` from test/utils.tsx to wrap component in AuthProvider + Router
    2. Mock the auth API if needed (login/logout endpoints)
    3. Set up authenticated state (mock localStorage or mock API responses)
    4. Dispatch `window.dispatchEvent(new CustomEvent('auth:unauthorized'))`
    5. Assert auth state is cleared (check hook return values via a test component)
    6. For unmount test: render, unmount with `cleanup`, dispatch event, verify no errors

    Run the tests. They should describe expected behavior. Commit as RED: `test(01-04): add failing tests for useAuth event handling`
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/frontend && npm test -- useAuth.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - useAuth.test.tsx contains tests for auth:unauthorized event handling
    - Tests run and either pass (if behavior already correct) or fail with clear expected behavior
    - Committed: `test(01-04): add failing tests for useAuth event handling`
  </done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: Verify and fix useAuth hook implementation</name>
  <files>frontend/src/hooks/useAuth.tsx</files>
  <read_first>
    - frontend/src/hooks/useAuth.tsx (current state)
    - frontend/src/hooks/useAuth.test.tsx (test results from RED phase)
  </read_first>
  <action>
    Run the tests from the RED phase. If any fail, fix useAuth.tsx minimally to pass.

    Likely issues to fix:
    1. If event listener test fails: verify the hook adds/removes the listener correctly
    2. If auth state clear test fails: verify state is cleared on event dispatch
    3. If redirect test fails: verify navigation occurs on logout

    Do NOT change behavior that is not tested. Only make minimal fixes to pass the tests.
    Commit as GREEN: `feat(01-04): verify useAuth event handling`
  </action>
  <verify>
    <automated>cd /home/thitar/dev/chore-ganizer/frontend && npm test -- useAuth.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All useAuth.test.tsx tests pass
    - Implementation is minimal (only changed if tests required it)
    - Committed: `feat(01-04): verify useAuth event handling`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → client | 401 responses trigger client-side logout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | client.ts error handling | accept | Error messages are already sanitized by backend; client just propagates them |
| T-04-02 | Denial of Service | useAuth event listener | accept | Event listener removal on unmount prevents memory leaks |
</threat_model>

<verification>
- All client.test.ts tests pass
- All useAuth.test.tsx tests pass
- `npm test` in frontend runs all tests without regressions
- `npm run build` compiles without errors
- `npm run lint` passes
</verification>

<success_criteria>
- Client error handling paths covered by automated tests
- useAuth hook responds correctly to auth:unauthorized events
- No regressions in existing test suite
- 2-3 atomic commits documenting TDD cycle
</success_criteria>

<output>
After completion, create `.planning/phases/01-remediate-codebase-concerns/01-04-SUMMARY.md`
</output>
