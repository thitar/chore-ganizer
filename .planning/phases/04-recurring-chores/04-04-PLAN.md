---
phase: 04-recurring-chores
plan: 04
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - frontend-v2/src/api/recurring.api.ts
  - frontend-v2/src/hooks/useRecurringChores.tsx
  - frontend-v2/src/pages/RecurringChoresPage.tsx
  - frontend-v2/src/components/NavBar.tsx
  - frontend-v2/src/App.tsx
  - frontend-v2/src/__tests__/RecurringChoresPage.test.tsx
autonomous: true
requirements: [RECUR-01, RECUR-04]
must_haves:
  truths:
    - "Parent sees Recurring Chores page at /recurring-chores with create and delete"
    - "NavBar shows 'Recurring' link for PARENT role"
    - "Child cannot access /recurring-chores (403 via ProtectedRoute)"
  artifacts:
    - path: "frontend-v2/src/api/recurring.api.ts"
      exports: ["RecurringChore", "getAll", "create", "delete_"]
    - path: "frontend-v2/src/hooks/useRecurringChores.tsx"
      exports: ["useRecurringChores"]
    - path: "frontend-v2/src/pages/RecurringChoresPage.tsx"
      exports: ["RecurringChoresPage"]
  key_links:
    - from: "frontend-v2/src/pages/RecurringChoresPage.tsx"
      to: "frontend-v2/src/hooks/useRecurringChores.tsx"
      via: "useRecurringChores()"
---

<objective>
Frontend data layer and page for recurring chores: API client with D-17 mapping, React Query hook, RecurringChoresPage for parent management (create form, delete with confirmation), NavBar link, and App.tsx route wiring.

Purpose: The recurring chore management UI mirrors TemplatesPage — parents create recurring chores with frequency/assignee selection, view the list, and delete with confirmation. Occurrences appear automatically in MyChoresPage and Dashboard via Plan 04-03.
</objective>

<context>
@.planning/phases/04-recurring-chores/04-RESEARCH.md
@frontend-v2/src/api/templates.api.ts
@frontend-v2/src/api/assignments.api.ts
@frontend-v2/src/hooks/useTemplates.tsx
@frontend-v2/src/hooks/useAssignments.tsx
@frontend-v2/src/pages/TemplatesPage.tsx
@frontend-v2/src/pages/AssignmentsPage.tsx
@frontend-v2/src/components/NavBar.tsx
@frontend-v2/src/App.tsx
@frontend-v2/src/components/ProtectedRoute.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recurring.api.ts and useRecurringChores hook</name>
  <files>frontend-v2/src/api/recurring.api.ts, frontend-v2/src/hooks/useRecurringChores.tsx</files>
  <read_first>frontend-v2/src/api/templates.api.ts, frontend-v2/src/hooks/useTemplates.tsx</read_first>

  <action>
    Follow the templates.api.ts / useTemplates.tsx pattern exactly.

    **1. Create `frontend-v2/src/api/recurring.api.ts`:**
    - Axios instance: `axios.create({ baseURL: '/api/recurring', withCredentials: true })`
    - Export `RecurringChore` interface: `{ id: number, choreTemplateId: number, assignedToId: number, frequency: 'DAILY' | 'WEEKLY' | 'MONTHLY', dayOfWeek: number | null, dayOfMonth: number | null, createdById: number, createdAt: string, template: { id, title, points, category }, assignedTo: { id, name, color } }`
    - Export `getAll(): Promise<RecurringChore[]>` — `api.get('/')` → response.data.data
    - Export `create(data: { templateId: number, userId: number, frequency: string, dayOfWeek?: number, dayOfMonth?: number }): Promise<RecurringChore>` — D-17 mapping: `templateId → choreTemplateId`, `userId → assignedToId`
    - Export `delete_(id: number): Promise<{ deleted: true }>` — `api.delete(`/${id}`)` → response.data.data
    - Export `complete(id: number): Promise<RecurringChore>` — `api.post(`/${id}/complete`)` → response.data.data

    **2. Create `frontend-v2/src/hooks/useRecurringChores.tsx`:**
    - Export `useRecurringChores()` returning `{ recurringChores, isLoading, error, createRecurringChore, isCreating, deleteRecurringChore, isDeleting }`
    - useQuery with `queryKey: ['recurring-chores']`, `queryFn: recurringApi.getAll`
    - createMutation with wrapper: `const createRecurringChore = (data) => createMutation.mutateAsync(data)`
    - deleteMutation with wrapper: `const deleteRecurringChore = (id) => deleteMutation.mutateAsync(id)`
    - Both invalidate `['recurring-chores']` on success
    - Expose `isPending` as `isCreating`, `isDeleting`
  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit — no TypeScript errors in API or hook files</automated>
  </verify>

  <acceptance_criteria>
    - recurring.api.ts exports RecurringChore interface, getAll, create, delete_, complete
    - create() maps templateId→choreTemplateId, userId→assignedToId per D-17
    - useRecurringChores returns query data + 2 mutations with loading states
    - Query key: ['recurring-chores']
    - No TypeScript errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build RecurringChoresPage, update NavBar, wire App.tsx</name>
  <files>frontend-v2/src/pages/RecurringChoresPage.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/App.tsx, frontend-v2/src/__tests__/RecurringChoresPage.test.tsx</files>
  <read_first>frontend-v2/src/pages/TemplatesPage.tsx, frontend-v2/src/components/NavBar.tsx, frontend-v2/src/App.tsx</read_first>

  <behavior>
    - Test: renders loading spinner
    - Test: renders empty state with "No recurring chores yet" and "Create Recurring Chore" button
    - Test: renders recurring chore list with template name, frequency, assignee
    - Test: create form has template select, user select, frequency select with conditional dayOfWeek/dayOfMonth
    - Test: delete shows confirmation panel
    - Test: error state with retry
  </behavior>

  <action>
    **First, create `frontend-v2/src/__tests__/RecurringChoresPage.test.tsx`** — RED tests. Follow TemplatesPage test pattern.

    **Then, build RecurringChoresPage** following TemplatesPage pattern:

    - States: loading (spinner), error (retry), empty ("No recurring chores yet"), populated (list with frequency badges and delete icon)
    - Create form: template select, user select (filtered to CHILD from useUsers), frequency select. When frequency is WEEKLY, show dayOfWeek select (Monday-Sunday). When MONTHLY, show dayOfMonth input (1-31). DAILY: no extra field.
    - Frequency badge in list: "Daily" (blue), "Weekly on Monday" (purple), "Monthly on day 15" (green)
    - Each row shows: template name, frequency description, assignee name, delete icon
    - Delete flow: ConfirmDelete with "Delete this recurring chore? Future occurrences will be removed. Already-completed chores are kept."
    - Follow TemplatesPage styling: table grid, form card, success message as fixed toast

    **Update NavBar**: Add "Recurring" link between "Assignments" and the right side, gated on `user?.role === 'PARENT'`.

    **Update App.tsx**: Add route `/recurring-chores` with `<ProtectedRoute requiredRole="PARENT"><RecurringChoresPage /></ProtectedRoute>`.
  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit && npx vitest run — RecurringChoresPage tests pass, no TypeScript errors</automated>
  </verify>

  <acceptance_criteria>
    - RecurringChoresPage.test.tsx has 6+ test cases
    - Parent sees page at /recurring-chores with create/delete
    - NavBar shows "Recurring" link for PARENT only
    - Create form: frequency select shows conditional dayOfWeek/dayOfMonth
    - App.tsx has /recurring-chores route with ProtectedRoute requiredRole="PARENT"
    - All tests pass, TypeScript clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-4-08 | Elevation of Privilege | CHILD accesses /recurring-chores via URL | mitigate | ProtectedRoute requiredRole="PARENT" |
| T-4-09 | Information Disclosure | NavBar shows Recurring to CHILD | mitigate | user.role === 'PARENT' gate in NavBar |
</threat_model>

<verification>
`cd frontend-v2 && npx vitest run` — all tests pass.
</verification>

<success_criteria>
- API client + hook: type-safe, D-17 mapping, cache invalidation
- RecurringChoresPage: all states covered, create with conditional frequency fields, delete with confirmation
- NavBar: Recurring link for PARENT
- App.tsx: route wired with role gate
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/04-recurring-chores/04-04-SUMMARY.md` when done
</output>
