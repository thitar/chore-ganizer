---
phase: 04-recurring-chores
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend-v2/prisma/schema.prisma
  - backend-v2/prisma/seed.ts
  - backend-v2/src/routes/index.ts
autonomous: true
requirements: [RECUR-05]
must_haves:
  truths:
    - "RecurringChore model has assignedToId Int with User relation (no RecurringChoreAssignee table)"
    - "RecurringOccurrence model has pointsAwarded Int? field"
    - "RecurringOccurrence onDelete Cascade removed — service handles deletion"
    - "User model no longer has recurringAssignees field"
    - "Seed script creates 2 recurring chores with assignedToId pointing to Alice and Bob"
  artifacts:
    - path: "backend-v2/prisma/schema.prisma"
      provides: "Simplified RecurringChore with single assignee, RecurringOccurrence with pointsAwarded"
      contains: "assignedToId Int"
    - path: "backend-v2/prisma/seed.ts"
      provides: "Seed data with 2 recurring chores"
      contains: "recurringChore.create"
  key_links:
    - from: "backend-v2/prisma/schema.prisma"
      to: "RECUR-05"
      via: "Single assignedToId field replaces RecurringChoreAssignee table"
      pattern: "assignedToId"
---

<objective>
Simplify the Prisma schema for recurring chores: replace the RecurringChoreAssignee many-to-many table with a single `assignedToId` on RecurringChore (per RECUR-05), add `pointsAwarded` to RecurringOccurrence for completion snapshots, remove `onDelete: Cascade` on the RecurringOccurrence→RecurringChore relation so completed occurrences survive chore deletion, update User model relations, and seed 2 sample recurring chores.
</objective>

<context>
@.planning/phases/04-recurring-chores/04-RESEARCH.md
@backend-v2/prisma/schema.prisma
@backend-v2/prisma/seed.ts
@backend-v2/src/routes/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify RecurringChore schema and add pointsAwarded to RecurringOccurrence</name>
  <files>backend-v2/prisma/schema.prisma</files>
  <read_first>backend-v2/prisma/schema.prisma</read_first>

  <action>
    Per RESEARCH Schema Changes:

    1. **Drop RecurringChoreAssignee model entirely** — RECUR-05 mandates fixed assignment.

    2. **Add `assignedToId Int` to RecurringChore** with relation `assignedTo User @relation(fields: [assignedToId], references: [id])`.
       - Keep existing fields: id, choreTemplateId, frequency, dayOfWeek, dayOfMonth, createdById
       - Remove `assignees RecurringChoreAssignee[]` relation field

    3. **Add `pointsAwarded Int?` to RecurringOccurrence** after the `status` field. Same pattern as ChoreAssignment.

    4. **Remove `onDelete: Cascade`** from the RecurringOccurrence→RecurringChore relation. Replace with plain `@relation(fields: [recurringChoreId], references: [id])` (no onDelete). This ensures completed occurrences survive when the parent RecurringChore is deleted.

    5. **Update User model**: Remove `recurringAssignees RecurringChoreAssignee[]`. Add `assignedRecurringChores RecurringChore[]` (inverse of the new `assignedTo` relation). Keep `completedOccurrences RecurringOccurrence[]` as-is.
  </action>

  <verify>
    <automated>cd backend-v2 && npx prisma generate — no errors</automated>
  </verify>

  <acceptance_criteria>
    - RecurringChoreAssignee model no longer exists in schema.prisma
    - RecurringChore has `assignedToId Int` with `assignedTo User @relation(fields: [assignedToId], references: [id])`
    - RecurringOccurrence has `pointsAwarded Int?` after status field
    - RecurringOccurrence→RecurringChore relation has NO `onDelete: Cascade`
    - User model has `assignedRecurringChores` not `recurringAssignees`
    - `npx prisma generate` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update seed script and push schema</name>
  <files>backend-v2/prisma/seed.ts</files>
  <read_first>backend-v2/prisma/seed.ts, backend-v2/prisma/schema.prisma</read_first>

  <action>
    1. **Add recurring chore seeding** after PointLog seeding in seed.ts:
       Create 2 recurring chores:
       - Daily "Make Bed" for Alice (alice.id), template points 5, frequency "DAILY"
       - Weekly "Take Out Trash" for Bob (bob.id), template points 10, frequency "WEEKLY", dayOfWeek: 1 (Monday)

       First ensure templates with those titles exist OR create them before recurring chores. Use `prisma.recurringChore.create({ data: {...} })`.

    2. **Update console.log** to include recurring chore count — `'Seed complete: 4 users, 3 chore templates, 2 point logs, 2 recurring chores'`.

    3. **Run `npx prisma db push --accept-data-loss`** — this drops the RecurringChoreAssignee table, adds the new fields, and removes onDelete Cascade.

    4. **Run `npx prisma db seed`** — verify seed completes without errors.

    5. **Run `cd backend-v2 && npx jest -- src/__tests__/scaffold.test.ts`** — all 6 scaffold tests must pass.
  </action>

  <verify>
    <automated>cd backend-v2 && npx prisma db push --accept-data-loss && npx prisma db seed && npx jest -- src/__tests__/scaffold.test.ts — seed succeeds, 6 tests pass</automated>
  </verify>

  <acceptance_criteria>
    - seed.ts has 2 recurring chores (one daily, one weekly) with assignedToId pointing to children
    - console.log mentions "2 recurring chores"
    - `npx prisma db push --accept-data-loss` exits 0
    - `npx prisma db seed` exits 0
    - All 6 scaffold tests pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema→database | Schema migration drops RecurringChoreAssignee table — dev data loss acceptable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | Schema migration via db push | mitigate | `--accept-data-loss` flag explicitly acknowledged; dev database only; seed recreates data |
</threat_model>

<verification>
After both tasks: `cd backend-v2 && npx prisma generate` exits 0, `npx tsc --noEmit` no errors, all 6 scaffold tests pass.
</verification>

<success_criteria>
- Schema simplified: no RecurringChoreAssignee, single assignedToId on RecurringChore
- RecurringOccurrence has pointsAwarded for completion snapshots
- onDelete Cascade removed — service handles deletion logic
- Seed creates 2 recurring chores
- prisma generate + db push + db seed all succeed
</success_criteria>

<output>
Create `.planning/phases/04-recurring-chores/04-01-SUMMARY.md` when done
</output>
