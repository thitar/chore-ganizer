---
phase: 04-recurring-chores
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - frontend-v2/src/hooks/useAssignments.tsx
  - frontend-v2/src/api/assignments.api.ts
  - frontend-v2/src/api/recurring.api.ts
  - frontend-v2/src/pages/MyChoresPage.tsx
  - frontend-v2/src/__tests__/MyChoresPage.test.tsx
autonomous: true
requirements: [RECUR-03]
must_haves:
  truths:
    - "Mark Complete on a recurring occurrence calls the correct endpoint (POST /api/recurring/:id/complete)"
    - "Mark Complete on a regular assignment still calls POST /api/assignments/:id/complete"
    - "Completed occurrences show green status badge, no Mark Complete button"
  artifacts:
    - path: "frontend-v2/src/api/assignments.api.ts"
      provides: "Updated Assignment interface with type discriminator"
      contains: "type: 'REGULAR' | 'RECURRING'"
    - path: "frontend-v2/src/hooks/useAssignments.tsx"
      provides: "Updated completeAssignment that routes to correct endpoint based on type"
      contains: "type === 'RECURRING'"
  key_links:
    - from: "frontend-v2/src/pages/MyChoresPage.tsx"
      to: "frontend-v2/src/hooks/useAssignments.tsx"
      via: "completeAssignment(id, type)"
      pattern: "completeAssignment.*type"
---

<objective>
Update the frontend assignment hook and MyChoresPage to handle the `type` discriminator from Plan 04-03: route completion to the correct API endpoint (regular → /api/assignments/:id/complete, recurring → /api/recurring/:id/complete). Update the Assignment interface to include the `type` field. Run full test suites to verify the integration works end-to-end.

Purpose: This is the final wiring that makes recurring occurrences fully functional — without it, clicking "Mark Complete" on a recurring occurrence would call the wrong endpoint and fail.
</objective>

<context>
@.planning/phases/04-recurring-chores/04-RESEARCH.md
@frontend-v2/src/api/assignments.api.ts
@frontend-v2/src/api/recurring.api.ts
@frontend-v2/src/hooks/useAssignments.tsx
@frontend-v2/src/hooks/useRecurringChores.tsx
@frontend-v2/src/pages/MyChoresPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add type discriminator to Assignment interface and route completion correctly</name>
  <files>frontend-v2/src/api/assignments.api.ts, frontend-v2/src/hooks/useAssignments.tsx</files>
  <read_first>frontend-v2/src/api/assignments.api.ts, frontend-v2/src/hooks/useAssignments.tsx, frontend-v2/src/api/recurring.api.ts</read_first>

  <action>
    1. **Update `Assignment` interface** in `assignments.api.ts`: add `type?: 'REGULAR' | 'RECURRING'` field (optional — backward compat with existing code that doesn't use it).

    2. **Update `useAssignments` hook**: modify the `completeAssignment` wrapper to accept an optional `type` parameter. If `type === 'RECURRING'`, call `recurringApi.complete(id)` instead of `assignmentsApi.complete(id)`.
       ```typescript
       import * as recurringApi from '../api/recurring.api'

       const completeAssignment = (id: number, type?: string) => {
         if (type === 'RECURRING') {
           return completeRecurring.mutateAsync(id)
         }
         return completeMutation.mutateAsync(id)
       }
       ```
       Add a second useMutation for recurring completion, also invalidating `['assignments']` on success.

       Expose `isCompleting` as OR of both mutation states: `isCompleting: completeMutation.isPending || completeRecurring.isPending`.

  </action>

  <verify>
    <automated>cd frontend-v2 && npx tsc --noEmit — no TypeScript errors</automated>
  </verify>

  <acceptance_criteria>
    - Assignment interface has optional `type` field
    - completeAssignment(id, type) routes RECURRING to recurringApi.complete
    - isCompleting reflects both regular and recurring completion pending states
    - No TypeScript errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update MyChoresPage to pass type discriminator to complete, run full test suite</name>
  <files>frontend-v2/src/pages/MyChoresPage.tsx, frontend-v2/src/__tests__/MyChoresPage.test.tsx</files>
  <read_first>frontend-v2/src/pages/MyChoresPage.tsx</read_first>

  <action>
    1. **Update MyChoresPage**: in `handleComplete`, pass the assignment's `type` to `completeAssignment`:
       ```typescript
       await completeAssignment(assignment.id, (assignment as any).type)
       ```

    2. **Update MyChoresPage test**: mock `completeAssignment` to verify it receives `(id, type)` arguments. Add a test case: "completes a recurring occurrence — calls completeAssignment with type 'RECURRING'".

    3. **Run full test suites:**
       ```bash
       cd backend-v2 && npx jest
       cd frontend-v2 && npx vitest run
       ```
       All tests must pass.

    4. **Verify occurrences appear in assignments**: after running seed (which creates daily + weekly recurring chores), GET /api/assignments should return both regular and recurring items with their respective `type` values.
  </action>

  <verify>
    <automated>cd backend-v2 && npx jest && cd ../frontend-v2 && npx vitest run — all tests pass</automated>
  </verify>

  <acceptance_criteria>
    - MyChoresPage passes type to completeAssignment when handling complete
    - MyChoresPage tests updated and pass
    - All backend tests pass (86+)
    - All frontend tests pass (31+)
    - TypeScript: no errors in either project
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-4-10 | Tampering | Wrong completion endpoint called | mitigate | Type discriminator routes to correct API; backend ownership checks provide defense in depth |
</threat_model>

<verification>
Full backend + frontend test suites must pass.
</verification>

<success_criteria>
- Type discriminator wired through API → hook → page
- Recurring occurrences can be completed from MyChoresPage
- Regular assignments still work unchanged
- Full test suites pass
</success_criteria>

<output>
Create `.planning/phases/04-recurring-chores/04-05-SUMMARY.md` when done
</output>
