---
phase: 02-authentication
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend-v2/src/services/auth.service.ts
  - backend-v2/src/services/__tests__/auth.service.test.ts
  - backend-v2/src/middleware/auth.ts
  - backend-v2/src/middleware/__tests__/auth.test.ts
  - backend-v2/src/routes/auth.routes.ts
  - backend-v2/src/routes/__tests__/auth.routes.test.ts
  - backend-v2/src/routes/index.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "User can log in with valid email/password and receive a session"
    - "User can log out and session is destroyed"
    - "Invalid credentials return 401"
    - "Unauthenticated requests to protected routes return 401"
    - "CHILD user hitting PARENT-only endpoint receives 403"
  artifacts:
    - path: "backend-v2/src/services/auth.service.ts"
      provides: "Login, logout, getCurrentUser business logic"
      exports: ["login", "logout", "getCurrentUser"]
    - path: "backend-v2/src/middleware/auth.ts"
      provides: "Authentication and authorization middleware"
      exports: ["authenticate", "authorize"]
    - path: "backend-v2/src/routes/auth.routes.ts"
      provides: "Auth API endpoints"
      exports: ["POST /login", "POST /logout", "GET /me"]
  key_links:
    - from: "backend-v2/src/routes/auth.routes.ts"
      to: "backend-v2/src/services/auth.service.ts"
      via: "service calls in route handlers"
      pattern: "authService\\.(login|logout|getCurrentUser)"
    - from: "backend-v2/src/middleware/auth.ts"
      to: "backend-v2/src/services/auth.service.ts"
      via: "session validation"
      pattern: "req\\.session\\.userId"
---

<objective>
Implement backend authentication: login, logout, session management, and role-based authorization middleware.

Purpose: Enable users to authenticate and enforce role-based access control for all subsequent API endpoints.
Output: Working auth service, middleware, and routes with full test coverage.
</objective>

<execution_context>
@/home/thitar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/thitar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1-scaffold/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend-v2/prisma/schema.prisma:
```prisma
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
  password  String
  role      String   @default("CHILD")
  points    Int      @default(0)
  color     String   @default("#3B82F6")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

From backend-v2/src/middleware/errorHandler.ts:
```typescript
export class AppError extends Error {
  statusCode: number
  code?: string
  constructor(message: string, statusCode: number = 500, code?: string)
}
```

From backend-v2/src/config/prisma.ts:
```typescript
export const prisma = new PrismaClient()
```

From backend-v2/src/app.ts:
```typescript
// Session already configured:
app.use(session({
  secret: process.env.SESSION_SECRET || 'dev-secret',
  resave: false,
  saveUninitialized: false,
  cookie: {
    httpOnly: true,
    secure: false,
    sameSite: 'lax',
    maxAge: 24 * 60 * 60 * 1000,
  },
}))
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Auth Service (TDD)</name>
  <files>backend-v2/src/services/auth.service.ts, backend-v2/src/services/__tests__/auth.service.test.ts</files>
  <read_first>
    - backend-v2/prisma/schema.prisma (User model structure)
    - backend-v2/src/config/prisma.ts (prisma client export)
    - backend-v2/src/middleware/errorHandler.ts (AppError class)
    - backend-v2/prisma/seed.ts (seed data for test setup)
  </read_first>
  <behavior>
    - login(email, password): Returns user object (without password) for valid credentials
    - login(email, password): Throws AppError(401, 'Invalid credentials') for wrong email
    - login(email, password): Throws AppError(401, 'Invalid credentials') for wrong password
    - logout(session): Destroys session and returns success
    - getCurrentUser(userId): Returns user object (without password) for valid ID
    - getCurrentUser(userId): Throws AppError(404, 'User not found') for invalid ID
    - Password verification uses bcrypt.compare
  </behavior>
  <action>
    Implement auth.service.ts with three functions:

    1. `login(email: string, password: string)`:
       - Find user by email using prisma.user.findUnique
       - If not found, throw new AppError('Invalid credentials', 401)
       - Compare password with bcrypt.compare
       - If invalid, throw new AppError('Invalid credentials', 401)
       - Return user object without password field

    2. `logout(session: Session)`:
       - Call session.destroy() wrapped in a Promise
       - Return { success: true }

    3. `getCurrentUser(userId: number)`:
       - Find user by ID using prisma.user.findUnique
       - If not found, throw new AppError('User not found', 404)
       - Return user object without password field

    Use TDD: write tests first, then implement.
    Tests should mock prisma using jest.mock('@prisma/client').
  </action>
  <verify>
    <automated>cd backend-v2 && npm test -- --testPathPattern=auth.service</automated>
  </verify>
  <acceptance_criteria>
    - auth.service.ts exports login, logout, getCurrentUser functions
    - login returns user object without password on success
    - login throws AppError with statusCode 401 on invalid credentials
    - logout calls session.destroy()
    - getCurrentUser returns user without password
    - getCurrentUser throws AppError with statusCode 404 for invalid ID
    - All tests pass with `npm test -- --testPathPattern=auth.service`
  </acceptance_criteria>
  <done>Auth service implemented with full test coverage, all tests passing</done>
</task>

<task type="tdd">
  <name>Task 2: Auth Middleware (TDD)</name>
  <files>backend-v2/src/middleware/auth.ts, backend-v2/src/middleware/__tests__/auth.test.ts</files>
  <read_first>
    - backend-v2/src/middleware/errorHandler.ts (AppError class pattern)
    - backend-v2/src/services/auth.service.ts (getCurrentUser for session validation)
    - backend-v2/prisma/schema.prisma (User model with role field)
  </read_first>
  <behavior>
    - authenticate(req, res, next): Calls next() if session.userId exists
    - authenticate(req, res, next): Returns 401 if no session.userId
    - authorize(...roles): Returns middleware that calls next() if user.role is in allowed roles
    - authorize(...roles): Returns middleware that returns 403 if user.role is not in allowed roles
    - authorize('PARENT'): Allows PARENT role, rejects CHILD role
  </behavior>
  <action>
    Implement auth.ts with two middleware factories:

    1. `authenticate` middleware:
       - Check req.session.userId exists
       - If missing, return 401 with { success: false, error: { message: 'Authentication required' } }
       - If exists, call next()

    2. `authorize(...roles: string[])` middleware factory:
       - Returns middleware that checks req.session.role
       - If role not in allowed roles array, return 403 with { success: false, error: { message: 'Forbidden' } }
       - If role is allowed, call next()

    Note: The session type needs to be extended to include userId and role.
    Create a types/express-session.d.ts file to extend the Session interface.

    Use TDD: write tests first, then implement.
    Tests should mock req, res, next objects.
  </action>
  <verify>
    <automated>cd backend-v2 && npm test -- --testPathPattern=middleware/auth</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts exports authenticate and authorize functions
    - authenticate calls next() when session.userId exists
    - authenticate returns 401 when session.userId is missing
    - authorize('PARENT') calls next() for PARENT role
    - authorize('PARENT') returns 403 for CHILD role
    - authorize('PARENT', 'CHILD') calls next() for both roles
    - All tests pass with `npm test -- --testPathPattern=middleware/auth`
  </acceptance_criteria>
  <done>Auth middleware implemented with full test coverage, all tests passing</done>
</task>

<task type="tdd">
  <name>Task 3: Auth Routes (TDD)</name>
  <files>backend-v2/src/routes/auth.routes.ts, backend-v2/src/routes/__tests__/auth.routes.test.ts, backend-v2/src/routes/index.ts</files>
  <read_first>
    - backend-v2/src/routes/health.routes.ts (route pattern)
    - backend-v2/src/services/auth.service.ts (service functions)
    - backend-v2/src/middleware/auth.ts (authenticate middleware)
    - backend-v2/src/routes/index.ts (router mounting pattern)
  </read_first>
  <behavior>
    - POST /api/auth/login with valid credentials: Returns 200 with user data, sets session
    - POST /api/auth/login with invalid credentials: Returns 401 with error message
    - POST /api/auth/logout: Returns 200, destroys session
    - GET /api/auth/me with valid session: Returns 200 with user data
    - GET /api/auth/me without session: Returns 401
    - All responses follow { success, data, error } envelope
  </behavior>
  <action>
    Implement auth.routes.ts with three endpoints:

    1. `POST /login`:
       - Extract email, password from req.body
       - Call authService.login(email, password)
       - Set req.session.userId = user.id and req.session.role = user.role
       - Return 200 with { success: true, data: user, error: null }

    2. `POST /logout`:
       - Call authService.logout(req.session)
       - Return 200 with { success: true, data: { message: 'Logged out' }, error: null }

    3. `GET /me`:
       - Use authenticate middleware
       - Get userId from req.session.userId
       - Call authService.getCurrentUser(userId)
       - Return 200 with { success: true, data: user, error: null }

    Mount auth router in routes/index.ts: `router.use('/auth', authRouter)`

    Use TDD: write tests first, then implement.
    Tests should use supertest to test the full request cycle.
    Mock authService to isolate route testing.
  </action>
  <verify>
    <automated>cd backend-v2 && npm test -- --testPathPattern=routes/auth</automated>
  </verify>
  <acceptance_criteria>
    - auth.routes.ts exports router with login, logout, me endpoints
    - POST /login returns 200 with user data on valid credentials
    - POST /login returns 401 on invalid credentials
    - POST /logout returns 200 and destroys session
    - GET /me returns 200 with user data when authenticated
    - GET /me returns 401 when not authenticated
    - All responses follow { success, data, error } envelope
    - routes/index.ts mounts auth router at /auth
    - All tests pass with `npm test -- --testPathPattern=routes/auth`
  </acceptance_criteria>
  <done>Auth routes implemented with full test coverage, all tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input (email, password) crosses here |
| session→middleware | Session data used for authorization decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Auth login | mitigate | bcrypt password comparison, generic "Invalid credentials" error (no user enumeration) |
| T-02-02 | Tampering | Session | mitigate | httpOnly session cookie, server-side session storage |
| T-02-03 | Repudiation | Auth actions | accept | No audit logging in V1 (out of scope per REQUIREMENTS.md) |
| T-02-04 | Info Disclosure | Auth responses | mitigate | Generic error messages, no stack traces in production |
| T-02-05 | Denial of Service | Auth endpoints | mitigate | Rate limiter already configured in app.ts (100 req/15min) |
| T-02-06 | Elevation of Privilege | Role middleware | mitigate | authorize() middleware checks role from session, not user input |
</threat_model>

<verification>
1. Run all auth tests: `cd backend-v2 && npm test -- --testPathPattern=auth`
2. Verify session persistence: Start server, login, make request without credentials, verify session works
3. Verify role enforcement: Login as CHILD, attempt PARENT-only endpoint, verify 403
</verification>

<success_criteria>
- POST /api/auth/login with valid credentials returns 200 and sets session cookie
- POST /api/auth/login with invalid credentials returns 401
- POST /api/auth/logout returns 200 and clears session
- GET /api/auth/me with valid session returns user data
- GET /api/auth/me without session returns 401
- authorize('PARENT') middleware returns 403 for CHILD users
- All unit and integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-01-SUMMARY.md`
</output>
