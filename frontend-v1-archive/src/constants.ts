/**
 * Frontend configuration constants
 * Centralized configuration for UI behavior and polling intervals
 */

// Notification polling interval in milliseconds
// Adjust this value based on desired notification refresh rate
export const NOTIFICATION_POLL_INTERVAL_MS = 30000
